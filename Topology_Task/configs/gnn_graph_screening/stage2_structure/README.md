# Stage 2: Structural Screen

This folder is intentionally populated only after the stage-1 winner is known.
Run `tools/gnn_graph_screening.py promote-structure WINNER.toml` to create the
eight runnable configs, `manifest.csv`, and `launch_all.sh` here.

