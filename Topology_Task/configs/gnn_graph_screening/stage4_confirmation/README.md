# Stage 4: Full-Budget Confirmation

This folder is populated after screening. Run
`tools/gnn_graph_screening.py confirm SELECTED.toml [...]` to create 15M-step
seed-0/1/2 confirmation configs, `manifest.csv`, and `launch_all.sh` here.
