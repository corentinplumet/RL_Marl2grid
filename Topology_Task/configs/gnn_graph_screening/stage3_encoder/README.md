# Stage 3: Encoder Screen

This folder is intentionally populated only after the stage-2 winner is known.
Run `tools/gnn_graph_screening.py promote-encoder WINNER.toml` to create the five
runnable encoder configs, `manifest.csv`, and `launch_all.sh` here.

