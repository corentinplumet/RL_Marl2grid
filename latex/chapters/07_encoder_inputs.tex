\chapter{What the Shared Encoder Reads}
\label{ch:encoder-inputs}

One encoder serves every agent, and the same encoder is what a transfer study
moves between grids. Its inputs are raw physical quantities, so before either
form of sharing can be interpreted the inputs themselves have to be measured.
This chapter does that: it establishes what the encoder consumes, how far those
distributions differ between two grids, which of the differences a network
absorbs and which it does not, and what corrections follow.
\\
\\
Chapter~\ref{ch:wcci-transfer} then applies the corrections and runs the
transfer. The measurements here are the reason that study is interpretable at
all: without them, a deficit under transfer could not be told apart from a
representation reading miscalibrated inputs.

\section{Why the input distribution matters}
\label{sec:wcci-why-distribution}

The graph encoder is grid-size independent and can therefore be transferred
across grids. A conventional action head cannot: it has one output per action
index, so its shape changes with the agent partitions and action sets. The
candidate scorer of Section~\ref{sec:candidate-action-scoring} escapes that by
applying shared weights and action-conditioned pooling to a variable set of
actions, which makes the action head transferable too. Only the flat centralized
critic stays grid-specific, its input dimension being set by the grid.
\\
\\
The two input paths must be distinguished. The critic receives the normalized
flat observation; the transferred actor encoder receives only graph node and
edge features, for which physical scaling and running standardization are
disabled here. A frozen \texttt{bus14} encoder therefore reads WCCI's raw graph
quantities, and whether that matters depends on how closely the two
distributions match.


\section{Measurement protocol}
\label{sec:wcci-protocol}

Both environments were rolled out under a do-nothing policy for 6{,}000
environment steps, with a cap of 300 steps per chronic so the sample spreads
over roughly twenty scenarios per grid rather than concentrating in one. At each
step the per-agent local graphs were rebuilt and every node and active physical
edge recorded, pooling agents together because a single encoder is shared across
them. This yields 222{,}000 node observations for \texttt{bus14} and 552{,}000
for WCCI, and 312{,}000 and 899{,}494 edge observations respectively.
\\
\\
Four statistics summarize each feature. Writing $F_A$ and $F_B$ for the two
empirical distributions with means $\mu_A,\mu_B$ and standard deviations
$\sigma_A,\sigma_B$, and $\sigma_p=\sqrt{(\sigma_A^2+\sigma_B^2)/2}$ for their
pooled spread:
\begin{align}
  \text{offset} &= \frac{\mu_B-\mu_A}{\sigma_p},
  &\text{scale ratio} &= \frac{\sigma_B}{\sigma_A}, \label{eq:offset-scale} \\
  \mathrm{KS} &= \sup_x |F_A(x)-F_B(x)|,
  &W_1/\sigma_p &= \frac{1}{\sigma_p}\int_0^1 |F_A^{-1}(q)-F_B^{-1}(q)|\,\mathrm{d}q .
\end{align}
{\color{clarif}
\paragraph{Reading the symbols.} $A$ is always \texttt{bus14}, the source grid,
and $B$ always WCCI, the target: an offset is therefore positive when WCCI sits
higher, and a scale ratio above one means WCCI is the wider of the two. $F_A$
and $F_B$ are the two \emph{empirical cumulative distribution functions}, one
per grid, built from the samples of Section~\ref{sec:wcci-protocol}: $F_A(x)$ is
the fraction of \texttt{bus14}'s sampled values that fall at or below $x$, so it
rises from $0$ to $1$ as $x$ sweeps the axis. Its inverse $F_A^{-1}(q)$ runs the
other way, returning the value below which a fraction $q$ of the sample lies ---
$F_A^{-1}(0.5)$ is the median. The two definitions are what let the last two
statistics compare the same pair of samples from perpendicular directions: KS
measures the gap between the curves vertically at fixed $x$, $W_1$ between their
inverses horizontally at fixed $q$.

\paragraph{Naming the last two.} $\mathrm{KS}$ is the Kolmogorov--Smirnov
statistic: the largest vertical gap between the two empirical cumulative
distribution functions. It lies in $[0,1]$, reaching $0$ when the distributions
coincide and $1$ when their supports are disjoint, and the last panel of
Figure~\ref{fig:wcci-distributions} draws the gap it measures. $W_1$ is the
$1$-Wasserstein distance, also called the earth mover's distance: the average
horizontal gap between the two quantile functions, or equivalently the least
mass$\,\times\,$distance needed to rearrange one distribution into the other.
The subscript is the exponent applied to that displacement --- $W_2$ would
square it and so weigh far excursions more heavily --- and this work uses
$p=1$ throughout. Because $W_1$ carries the units of the feature, it is reported
in pooled standard deviations so that channels measured in MW and in degrees
stay comparable.}
\\
\\
The pairing is deliberate: the offset is blind to a change in spread and the
scale ratio is blind to a displacement, so a channel is only unproblematic when
both sit near their neutral values. KS measures disagreement in the bulk and is
comparatively insensitive to tails, which $W_1$ does weigh.
Section~\ref{sec:wcci-interpretation} shows why the two failures need separating.


\section{What the encoder reads}
\label{sec:wcci-features}

The measurements are taken on the busbar graph, whose ten channels are
specified in Tables~\ref{tab:graph-node-features}
and~\ref{tab:graph-edge-features} and repeated with their units in
Table~\ref{tab:wcci-graph-features-appendix}. What matters here is not their
definition but how often each is zero, because four of the six node channels are
structurally sparse: a busbar carries generation or load only when an asset is
attached to it, and an angle is recorded only where that asset connects.
\\
\\
The zero fraction is a sparsity diagnostic, not an information score. 100\,\%
means every sampled value is zero, whereas 0\,\% only means zero was never
observed: \texttt{line\_status} is constant at one because the measurement
retains only active physical edges, and \texttt{rho} is nonzero on every
retained edge. The overflow channel is not constant but nearly so, with 8
nonzero entries out of 312{,}000 on \texttt{bus14} and 6 out of 899{,}494 on
WCCI. The two cooldown channels are exactly zero only because the sample uses a
do-nothing policy; under control a topology change sets the substation cooldown
to 3 and a reconnection sets the line cooldown to 3.
\\
\\
One feature is dimensionless by construction: \texttt{rho} divides each line's
flow by that line's own thermal limit. Every other physical channel carries the
units of the grid it came from.


\section{Results}
\label{sec:wcci-results}

Table~\ref{tab:wcci-shift} reports the shift statistics twice: over all sampled
entries, and over only the nodes that carry the asset in question. The two
readings differ because a sparse channel is a point mass at zero mixed with a
continuous part, and the aggregate averages the two together.

\begin{table}[H]
  \centering
  \small
  \caption{Distribution shift between the two grids, ordered by KS. Offset is in
  pooled standard deviations and the scale ratio is WCCI over \texttt{bus14}.
  The right-hand block restricts the four sparse channels to nodes carrying the
  corresponding asset. {\color{clarif}\emph{dense} marks a channel with no
  structural zeros to remove: every entry is a genuine measurement, so the
  restriction would return the same rows and the same numbers. \emph{constant}
  marks a channel with no variation in this sample, whose offset and ratio are
  therefore undefined.} Means, standard deviations and $W_1$ for both readings
  are in Table~\ref{tab:wcci-shift-full-appendix}.}
  \label{tab:wcci-shift}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{lrrrrrrrr}
    \toprule
    & \multicolumn{2}{c}{Zeros (\%)} & \multicolumn{3}{c}{All sampled entries}
    & \multicolumn{3}{c}{Asset-carrying nodes} \\
    \cmidrule(lr){2-3}\cmidrule(lr){4-6}\cmidrule(lr){7-9}
    Channel & \texttt{bus14} & WCCI & offset & ratio & KS & offset & ratio & KS \\
    \midrule
    \texttt{load\_theta}   & 45.9 & 48.9 & $+1.194$ & 0.90 & 0.412 & $+2.105$ & 1.65 & \textbf{0.757} \\
    \texttt{gen\_theta}    & 81.1 & 76.1 & $+0.566$ & 0.93 & 0.139 & $+1.756$ & 1.49 & \textbf{0.723} \\
    \texttt{gen\_p}        & 80.1 & 85.4 & $+0.017$ & 2.45 & 0.111 & $+0.255$ & \textbf{5.84} & 0.497 \\
    \texttt{load\_p}       & 45.9 & 46.7 & $-0.078$ & 2.42 & 0.055 & $-0.105$ & 2.87 & 0.093 \\
    \midrule
    \texttt{rho}           & 0 & 0 & $-0.259$ & 1.05 & 0.141 & \multicolumn{3}{c}{dense} \\
    \texttt{domain\_mask}  & 24.3 & 21.7 & $+0.061$ & 0.96 & 0.026 & \multicolumn{3}{c}{dense} \\
    \texttt{timestep\_overflow} & \multicolumn{2}{c}{$>99.99$} & $-0.004$ & 0.70 & 0.000 & \multicolumn{3}{c}{dense} \\
    \texttt{line\_status}  & 0 & 0 & --- & --- & 0.000 & \multicolumn{3}{c}{constant} \\
    \texttt{time\_before\_cooldown\_line} & 100 & 100 & --- & --- & 0.000 & \multicolumn{3}{c}{constant} \\
    \texttt{time\_before\_cooldown\_sub}  & 100 & 100 & --- & --- & 0.000 & \multicolumn{3}{c}{constant} \\
    \bottomrule
  \end{tabular}
\end{table}

\noindent Read carelessly the left block looks mild: only \texttt{load\_theta} exceeds
$\mathrm{KS}=0.2$ and the power channels sit near $\mathrm{KS}=0.1$. Their scale
ratios, however, are 2.4 --- two distributions sharing a centre while differing
in width, which is precisely what KS alone cannot see.
\\
\\
The right block is the comparison the weights respond to, and it widens every
shift sharply: \texttt{load\_theta} moves from $0.412$ to $0.757$ in KS and from
$0.90$ to $1.65$ in scale ratio, and \texttt{gen\_p}'s spread ratio goes from
$2.45$ to $5.84$. Restricting to asset-carrying nodes removes the structural
zeros the two grids share, leaving only the physics on which they differ. This
is why the mixture structure matters for any per-feature normalization, a point
Section~\ref{sec:wcci-running-norm} returns to.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figures/wcci_feature_distributions.png}
  \caption{The channels the encoder reads, with the four sparse ones restricted
  to nodes carrying the corresponding asset. \texttt{rho} lands almost on top of
  itself, as expected from a quantity already normalized by each grid's own
  thermal limits. The power channels share a centre but not a width, and WCCI
  alone produces negative \texttt{load\_p}. The angles are displaced rather than
  rescaled: \texttt{bus14} sits almost entirely on the negative side. The last
  panel reads that displacement as the KS statistic. Tails beyond the plotted
  range appear on log density in
  Figure~\ref{fig:wcci-tails-appendix}.}
  \label{fig:wcci-distributions}
\end{figure}


\section{Two failure modes}
\label{sec:wcci-interpretation}

The left panel of Figure~\ref{fig:wcci-offset-scale} separates the channels
along the two axes; the right panel repeats the diagnostic after the corrections
and is read in Section~\ref{sec:wcci-corrected}. The distinction is not
cosmetic, because the architecture responds to the two axes differently.

{\color{clarif}
\paragraph{Reading the axes.} Each point is one channel, placed by the two
statistics of Equation~\eqref{eq:offset-scale} taken over all sampled entries. The horizontal axis
is the \emph{magnitude} of the offset, $|\mu_B-\mu_A|/\sigma_p$, in pooled
standard deviations: how far the two distributions have moved apart, with the
sign discarded because only the size of a displacement matters here. The
vertical axis is the scale ratio $\sigma_B/\sigma_A$, WCCI over \texttt{bus14}:
how much wider or narrower WCCI's spread is, with $1$ meaning identical spread.
It is drawn on a logarithmic scale so that halving and doubling sit the same
distance from $1$, which a linear axis would misrepresent. A channel needing no
correction sits at the origin of this reading --- near-zero offset and ratio
near $1$ --- so distance from the bottom-left corner is distance from
comparability, and the direction of that distance names which of the two
failures it is.}
\\
\\

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figures/wcci_feature_offset_scale.png}
  \caption{Absolute offset against scale ratio, over all sampled entries. The
  shaded band and the dashed line mark a scale ratio within a third of unity;
  the dotted vertical line marks an offset of $0.3$ pooled standard deviations.
  \emph{Left:} as measured. Channels outside those bounds fail in one of two
  distinct ways --- the power channels keep their centre and widen, the angles
  keep their width and move. \emph{Right:} the same diagnostic after the two
  corrections of Section~\ref{sec:wcci-correction}, discussed in
  Section~\ref{sec:wcci-corrected}. Every channel now sits inside the scale
  band, and the worst displacement falls from $1.19$ to $0.48$, but the power
  channels acquire an offset they did not have.}
  \label{fig:wcci-offset-scale}
\end{figure}

\noindent The actor's node pre-encoder is a linear map followed by ReLU and LayerNorm.
LayerNorm absorbs much of a uniform change of \emph{scale}: multiplying every
input channel by a constant leaves the normalized activations largely unchanged.
It does not absorb an \emph{offset}. Displacing a channel by a constant moves
the pre-activations to a different region of the ReLU, changing which units fire
at all, and normalization applied afterwards cannot undo that.
\\
\\
This makes the angles, not the powers, the binding problem. The power channels
differ in width by factors of 2.9 to 5.8, which the architecture partially
self-corrects, though the negative \texttt{load\_p} region on WCCI reaching
$-281$~MW is genuinely unseen territory rather than a wider version of something
familiar. The angles differ by roughly two pooled standard deviations of
displacement, which it does not correct at all.
\\
\\
\texttt{rho}, meanwhile, needs no correction at all: scale ratio 1.050, offset
$-0.26$. The single most decision-relevant channel is already comparable across
grids because it was defined in per-unit terms --- an argument for expressing
features in grid-relative units wherever the physics permits it.


\section{Correcting the inputs}
\label{sec:wcci-correction}

Two preprocessing mechanisms exist in the codebase. Deterministic physical
scaling divides each channel by a per-environment physical constant. Running
standardization maintains per-environment, per-node-type statistics over both
power and both angle channels, excluding \texttt{rho}, which the measurements
above confirm needs no help. On the diagnosis alone the second looks like the
match, being the only one that can move a displaced channel. It is not.

\subsection{Running standardization makes the mismatch worse}
\label{sec:wcci-running-norm}

Standardizing each environment with its own statistics aligns the aggregate
moments exactly: every channel comes out at offset $0.00$ and scale ratio
$1.00$. On three of the four channels it touches it nonetheless moves the
distributions several times further apart (Table~\ref{tab:wcci-running-norm}).

\begin{table}[H]
  \centering
  \small
  \caption{KS between the two grids before and after per-environment
  standardization. The moments align perfectly, and three of the four channels
  degrade by a factor of five or more. {\color{clarif}The last column is the
  number a busbar with \emph{no asset attached} presents to the encoder once
  standardized. Raw, that busbar reads $0$ on both grids. Standardizing sends it
  to $(0-\mu)/\sigma=-\mu/\sigma$, which is a property of the grid, so the same
  physical fact arrives as two different numbers --- and on the angle channels
  with two different signs.}}
  \label{tab:wcci-running-norm}
  \setlength{\tabcolsep}{8pt}
  \begin{tabular}{lrrc}
    \toprule
    Channel & KS raw & KS standardized & ``No asset'' encoding \\
    & & & \texttt{bus14} vs WCCI \\
    \midrule
    \texttt{gen\_p}      & 0.111 & \textbf{0.805} & $-0.450$ vs $-0.197$ \\
    \texttt{gen\_theta}  & 0.139 & \textbf{0.700} & $+0.409$ vs $-0.148$ \\
    \texttt{load\_p}     & 0.055 & \textbf{0.590} & $-0.599$ vs $-0.188$ \\
    \texttt{load\_theta} & 0.412 & 0.326 & $+0.910$ vs $-0.252$ \\
    \bottomrule
  \end{tabular}
\end{table}

\noindent The cause is the mixture structure documented in
the right-hand block of Table~\ref{tab:wcci-shift}. These channels are a point mass at zero,
meaning no generator or load is attached to this busbar, plus a continuous part.
Subtracting a per-grid mean moves that point mass to a grid-dependent location.
Since 80--85\,\% of busbars carry no generator, the encoder's single most
frequent input stops meaning the same thing on the two grids, which is a larger
mismatch than the displacement being corrected. On the two angle channels it is
starker still: an unattached busbar reads $+0.409$ and $+0.910$ on
\texttt{bus14} but $-0.148$ and $-0.252$ on WCCI, so the absence of an asset
lands on opposite sides of zero depending on which grid the encoder is reading.
\\
\\
The one channel that improves confirms the mechanism rather than contradicting
it. \texttt{load\_theta} alone has grids that agree on how often it is zero
($45.9$ against $48.9$\,\%) and a raw scale ratio already near unity, so its
point mass moves by nearly the same amount on both sides and standardization
does little there but remove the offset. The three that degrade lack exactly
that coincidence. Even so it is not the better fix: the representation change of
Section~\ref{sec:wcci-angle-representation} brings the same channel to $0.230$
without touching the point mass at all.
\\
\\
Two further properties rule the mechanism out independently of these numbers.
Standardization is scale-invariant, so it erases any scaling applied before it
--- dividing by a constant and then standardizing reproduces standardizing alone
to four decimals --- meaning the two mechanisms cannot be combined. And running
statistics begin empty and converge during training, so a frozen encoder would
spend early target training reading a distribution that is itself still moving,
where a deterministic scaling is fixed at construction and identical on both
grids from the first step.

\subsection{Physical scaling for the power channels}
\label{sec:wcci-physical-scaling}

Physical scaling, defined in Section~\ref{subsec:graph-feature-normalization},
divides the power channels by each environment's own maximum generator capacity.
Two properties answer the failure diagnosed above: being multiplicative it
leaves zeros at zero, so the point mass stays aligned on both grids, and being
fixed at construction it does not drift the way estimated moments do. The
divisor is a single generator's nameplate rating, a crude proxy for grid scale,
so it is worth checking against the alternatives
(Table~\ref{tab:wcci-normalizer}); it is the closest of the four to the divisor
that would equalize the two spreads exactly.

\begin{table}[H]
  \centering
  \small
  \caption{Candidate power normalizers and the \texttt{gen\_p} scale ratio each
  would produce. A ratio of $1.00$ would be exact; the raw ratio is $2.45$.}
  \label{tab:wcci-normalizer}
  \setlength{\tabcolsep}{8pt}
  \begin{tabular}{lrrr}
    \toprule
    Divisor & \texttt{bus14} & WCCI & Resulting ratio \\
    \midrule
    Exact & --- & --- & 1.00 \\
    \textbf{Maximum $P_{\max}$ (used)} & 140 & 400 & \textbf{0.86} \\
    Total $P_{\max}$ & 540 & 2{,}450 & 0.54 \\
    Generator count & 6 & 22 & 0.67 \\
    Median $P_{\max}$ & 85 & 71 & 2.93 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{The angle displacement is a gauge artifact}
\label{sec:wcci-angle-representation}

Scaling cannot repair the angle displacement, and the implementation does not
attempt it: angle channels are divided by a constant $180$, identical on both
grids, so both sides move together and the displacement is untouched. The
measurement explains why. An absolute voltage angle is referenced to whichever
bus is slack, and the two grids do not place theirs alike, so the $5$ to
$7^{\circ}$ displacement in Figure~\ref{fig:wcci-distributions} is largely gauge rather
than physics. Nothing that rescales or recentres a reference-dependent quantity
can fix that; the quantity has to be replaced.
\\
\\
The replacement is the angle drop along each line, defined in
Section~\ref{sec:line-node-angle}. Being a difference it is gauge-invariant, and
being per line it is dense, so it removes the mixture structure from the angle
channels outright rather than working around it. This study therefore enables
it, alongside physical scaling, in both source and target environments.

\subsection{The action descriptor must not be normalized per agent}
\label{sec:wcci-action-descriptor}

One input reaches the actor without passing through the encoder: the
candidate-action head of Section~\ref{sec:candidate-action-scoring} appends a
static descriptor of each action to the pooled node context. Its count columns
were divided by the largest value in that agent's \emph{own} action set --- the
construction Section~\ref{sec:wcci-running-norm} rejects for node features,
applied to a different input, with the agents as the populations.

\begin{table}[H]
  \centering
  \small
  \caption{Divisors applied to the action count descriptors under the previous
  per-agent rule, \texttt{bus14}.}
  \label{tab:action-descriptor-divisors}
  \setlength{\tabcolsep}{7pt}
  \begin{tabular}{lrrr}
    \toprule
    Agent & Actions & Topology changes & Line endpoints \\
    \midrule
    0 & 57 & 5 & 3 \\
    1 & 51 & 5 & 4 \\
    2 & 83 & 6 & 3 \\
    \bottomrule
  \end{tabular}
\end{table}

\noindent An action moving three elements therefore reached the shared scorer as
$0.60$ for agent~$0$ and $0.50$ for agent~$2$, and every agent's busiest action
reached it as exactly $1.0$ whether it moved five elements or six. On another
grid the divisors change again. The columns are now divided by a constant,
selected by \texttt{candidate\_action\_feature\_scaling}; the busiest actions
then read $1.25$, $1.25$ and $1.5$. This is the only correction here that
applies within a grid as well as across grids, and it qualifies Screen~E of
Chapter~\ref{ch:experiments}, whose runs predate it: their \texttt{f1} cells
appended a descriptor inconsistent between the agents sharing one scorer.

\subsection{The corrected distribution}
\label{sec:wcci-corrected}

Table~\ref{tab:wcci-corrected} measures both changes together, under the same
protocol as Section~\ref{sec:wcci-protocol}, and the right panel of
Figure~\ref{fig:wcci-offset-scale} places the result back on the offset--scale
diagnostic. The angle channel lands in the band occupied by \texttt{rho}, the
power spreads come within 15\,\% of parity, and every channel now sits inside
the scale band that only \texttt{rho} and \texttt{domain\_mask} occupied before.

\begin{table}[H]
  \centering
  \small
  \caption{Distribution shift with \texttt{gnn\_angle\_representation =
  edge\_diff} and \texttt{gnn\_physical\_scaling = true}, against the
  uncorrected baseline, over all sampled entries. The offset column is included
  because scaling moves it: dividing each grid by its own constant is not
  offset-preserving when the two constants stand in a different relation to
  typical operation.}
  \label{tab:wcci-corrected}
  \setlength{\tabcolsep}{6pt}
  \begin{tabular}{lrrr|rrr}
    \toprule
    & \multicolumn{3}{c|}{Uncorrected} & \multicolumn{3}{c}{Corrected} \\
    \cmidrule(lr){2-4}\cmidrule(lr){5-7}
    Channel & offset & ratio & KS & offset & ratio & KS \\
    \midrule
    \texttt{load\_theta} & $+1.194$ & 0.90 & 0.412 & \multicolumn{3}{c}{replaced} \\
    \texttt{gen\_theta}  & $+0.566$ & 0.93 & 0.139 & \multicolumn{3}{c}{replaced} \\
    \texttt{theta\_diff} & \multicolumn{3}{c|}{---} & $-0.383$ & \textbf{1.15} & \textbf{0.230} \\
    \texttt{gen\_p}      & $+0.017$ & 2.45 & 0.111 & $-0.302$ & \textbf{0.86} & 0.145 \\
    \texttt{load\_p}     & $-0.078$ & 2.42 & 0.055 & $-0.476$ & \textbf{0.85} & 0.307 \\
    \texttt{rho}         & $-0.259$ & 1.05 & 0.141 & $-0.259$ & 1.05 & 0.141 \\
    \bottomrule
  \end{tabular}
\end{table}

\noindent The KS increase on the two power channels is the expected cost of the scaling
rather than a regression. Scaling slides the continuous part of a channel while
its point mass stays pinned at zero, so the two cumulative distributions
separate slightly in the bulk even as their dynamic ranges align. Dynamic range
is what a frozen encoder's weights are calibrated against, so the trade is
favourable, and it accounts for \texttt{load\_p} reading $0.307$.
\\
\\
The offset column records a second cost, and a less comfortable one. The raw
power means were nearly identical, at $+0.017$ and $-0.078$; after division they
are not. \texttt{bus14} runs its generators at $39\,\%$ of its fleet maximum
against WCCI's $20\,\%$, so a divisor taken from the largest unit lands the two
grids at different normalized levels and the displacement grows to $-0.302$ and
$-0.476$. Section~\ref{sec:wcci-interpretation} argued that offsets, not scale
errors, are what the architecture cannot absorb, so this is the correction
moving a channel onto the axis that matters rather than off it.
\\
\\
The exchange is still favourable on the evidence available: the worst
displacement across all channels falls from $1.194$ to $0.476$, the scale errors
of $2.4$ resolve to within $15\,\%$, and no channel remains outside both bounds
at once. But it is an exchange, not an elimination, and a divisor reflecting
typical rather than peak generation might do better on both axes at once.
Table~\ref{tab:wcci-normalizer} tested four candidates against the scale ratio
alone; selecting one against the offset as well is left open, and the transfer
results of Chapter~\ref{ch:wcci-transfer} are the test of whether the residual
displacement matters in practice.

\begin{figure}[H]
  \centering
  \includegraphics[width=\linewidth]{figures/wcci_feature_corrected.png}
  \caption{Corrected distributions, for comparison with
  Figure~\ref{fig:wcci-distributions}. The angle channel is
  now dense and centred on both grids, and the two power channels overlap over
  most of their range instead of differing by a factor of three in width.}
  \label{fig:wcci-corrected}
\end{figure}


