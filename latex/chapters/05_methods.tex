\chapter{Methods}
\label{ch:methods}


\revision{Chapter~\ref{ch:sparse-control} made the flat policy behave more like
an operator on \texttt{bus14}, but the first WCCI test was still weak. The sparse
control mechanisms changed when the policy acted, but they did not solve a more
basic problem: the MLP input and output sizes were tied to one grid. This
chapter replaces that fixed representation with graph-based actors that can be
used on grids of different sizes. It introduces the choices studied later in
the thesis: graph construction, substation augmentations, graph readout,
candidate-action scoring, cross-grid input transformations, and action-space
reduction.}

\section{Graph Representations}
\label{sec:graph-representation}

\revision{I use three graph representations. They mainly differ in which
physical objects receive their own node. The \emph{busbar} graph places all
measurements on busbar nodes. The \emph{disaggregated} graph adds separate
generator and load nodes, but still represents transmission lines as edges.
The \emph{disaggregated line-node} graph also turns each transmission line into
a node.}
\\
\\
\revision{Table~\ref{tab:schema-comparison} compares the three choices. The
representation also changes what a local actor can see at the far end of a
boundary line. The graphs therefore differ not only in size, but also in the
information they contain. Section~\ref{subsec:graph-local-global} describes
this boundary in detail.}

\begin{table}[H]
  \centering
  \scriptsize
  \caption{The three graph schemas. Each row follows from how much
  equipment receives its own node. In the node counts, $S$ is the number of
  substations, $B$ the busbars per substation, $N_g$ and $N_d$ the included
  generators and loads, and $L$ the included transmission lines.}
  \label{tab:schema-comparison}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{L{0.15\textwidth}L{0.24\textwidth}L{0.24\textwidth}L{0.25\textwidth}}
    \toprule
    Aspect & busbar & disaggregated & disaggregated line-node \\
    \midrule
    \texttt{gnn\_graph\_type} & \texttt{bus} & \texttt{heterogeneous} & \texttt{heterogeneous\_line} \\
    Node types & Busbar & Busbar, generator, load & Busbar, generator, load, transmission line \\
    Generator and load state & Summed and averaged onto the busbar & One node per asset & One node per asset \\
    Transmission line & Direct busbar-to-busbar edge & Typed direct busbar-to-busbar edge & Typed node between its endpoint busbars \\
    Line state & Continuous edge attributes & Continuous edge attributes & Features of the line node \\
    \revision{Shortest message path} & \revision{One step between adjacent busbars} & \revision{One step between adjacent busbars; two from an asset to a remote busbar} & \revision{Two steps between adjacent busbars through a line node} \\
    Hidden line state & None: the edge modifies a message & None: the edge modifies a message & Yes: each line obtains and updates an embedding \\
    Far-end context in a local actor graph & Live neighboring busbar, including aggregated power and angle & Live neighboring busbar, but no neighboring generator or load nodes & None; the shared line node replaces the need for a far-end busbar \\
    Node count & $SB$ & $SB+N_g+N_d$ & $SB+N_g+N_d+L$ \\
    \bottomrule
  \end{tabular}
\end{table}

\revision{The message-path row describes distances in each graph schema. It
does not set the encoder depth $K$, which is selected experimentally in
Chapter~\ref{ch:experiments}.}

% \subsection{Candidate graph and dynamic mask}
% \label{subsec:graph-candidate-edges}

% All three schemas share one construction.
% \\
% \\
% Grid2Op exposes the instantaneous electrical state and topology through a
% structured observation~\cite{donnot2020grid2op}. For each representation,
% the graph construction keeps the set of candidate nodes and edges fixed
% throughout an episode while using a dynamic edge mask to express the topology
% that is electrically active at time step~$t$.
% \\
% \\
% For agent~$a$, the graph observation can be written as
% \begin{equation}
%   \mathcal{G}^{(a)}_t =
%   \left(\mathcal{V}^{(a)},\mathcal{E}^{(a)}_{\mathrm{cand}},
%   \mathbf{X}^{(a)}_t,\mathbf{Z}^{(a)}_t,
%   \mathbf{m}^{(a)}_t,\mathbf{n}^{(a)}_t,
%   \mathbf{c}^{(a)}\right),
%   \label{eq:agent-graph-observation}
% \end{equation}
% where $\mathcal{V}^{(a)}$ and
% $\mathcal{E}^{(a)}_{\mathrm{cand}}$ are static, $\mathbf{X}^{(a)}_t$ contains
% node features, $\mathbf{Z}^{(a)}_t$ contains edge features,
% $\mathbf{m}^{(a)}_t$ indicates which candidate edges are active,
% $\mathbf{n}^{(a)}_t$ marks currently visible nodes, and
% $\mathbf{c}^{(a)}$ marks nodes belonging to the agent's controlled domain.
% The controlled mask is static, whereas the edge and visibility masks change
% with line status and busbar assignments. This separates the fixed candidate
% layout from the electrical topology that can change after every action.
% \\
% \\
% The mechanism is easiest to see on a transmission line in the busbar and
% disaggregated schemas, where a transmission line is an edge. Consider a
% line~$\ell$ whose origin and extremity
% belong to substations $o(\ell)$ and $e(\ell)$. For every pair of possible
% endpoint busbars $(b_o,b_e)$, the graph reserves the two directed candidate
% edges
% \begin{equation}
%   i(o(\ell),b_o)\rightarrow i(e(\ell),b_e)
%   \quad\text{and}\quad
%   i(e(\ell),b_e)\rightarrow i(o(\ell),b_o),
%   \label{eq:candidate-line-edges}
% \end{equation}
% so each transmission line contributes $2B^2$ candidate directed edges and the two
% directions share the same line attributes. For the usual case $B=2$, one line
% reserves eight candidate edges even though at most two, one per direction, are
% electrically active at any time.
% \\
% \\
% Which of them is active is decided by the mask
% \begin{equation}
%   m_{\ell,b_o,b_e,t}=
%   \mathbf{1}\!\left[\text{line }\ell\text{ is connected at }t\right]
%   \mathbf{1}\!\left[b^{\mathrm{or}}_{\ell,t}=b_o\right]
%   \mathbf{1}\!\left[b^{\mathrm{ex}}_{\ell,t}=b_e\right].
%   \label{eq:physical-edge-mask}
% \end{equation}
% A disconnected line has all of its candidates masked; a connected one keeps
% only the pair matching its current origin and extremity busbars, provided both
% endpoint nodes are visible. A topology action therefore changes connectivity
% by changing $\mathbf{m}^{(a)}_t$, never by rebuilding the graph, and the node
% and edge tensors keep a fixed shape for the whole episode. The same candidate
% and mask pattern governs the asset attachments of
% Section~\ref{sec:heterogeneous-graph-representation} and the line-node
% attachments of Section~\ref{sec:heterogeneous-line-graph};
% Appendix~\ref{app:graph-construction} gives the resulting sizes and the
% masking conventions.

% {\color{clarif}
% \paragraph{Why a fixed shape rather than rebuilding the graph.}
% A fixed candidate graph keeps every observation at the same tensor shape, so
% parallel rollouts can be stacked and stored without rebuilding or re-indexing
% the graph. Stable rows also allow each action to be mapped to the physical
% assets it modifies once, before training. Most importantly, masked candidates
% retain connections that an action could create; rebuilding only the active
% topology would omit empty destination busbars and hide feasible alternatives,
% which is the busbar information asymmetry identified
% by~\textcite{dejong2025gnntopo}. The trade-off is a larger edge tensor: with
% $B=2$, each line reserves eight directed candidates although at most two are
% active.
% }
% \\
% \\
% The following subsections define the three schemas.

\subsection{Busbar Representation}
\label{subsec:busbar-representation}

\revision{The busbar graph is the simplest and most compact representation.
Busbars are the only nodes and active transmission lines are the edges.
Generator and load measurements are added to the busbar to which each asset is
currently connected.}

\begin{figure}[htbp]
  \centering
  \resizebox{0.94\linewidth}{!}{\input{figures/graph_busbar_baseline.tex}}
  \caption{Busbar graph representation. Blue nodes are busbars and black
  connections are active transmission-line relations. Generator and load
  quantities are aggregated into the features of their associated busbars.}
  \label{fig:busbar-graph-representation}
\end{figure}

\subsubsection{Nodes and features}
\label{subsec:graph-busbar-nodes}

For $S$ substations and at most $B$ busbars per substation, the graph
contains one node $i(s,b)$ for every substation--busbar pair.
\latestrevision{Generator and load powers are summed on their current busbar,
their angles are averaged, and the substation cooldown is copied to both
busbars. The complete feature inventory is reported in
Appendix~\ref{sec:gnn-node-input-inventory}.}

\subsubsection{Relations and edge features}
\label{subsec:graph-line-edge-features}

\revision{The only physical relation in this graph is a transmission line
between two busbars.} \latestrevision{Each active line edge carries its status,
loading, overload duration, and cooldown, together with maintenance timing when
that information is available. Appendix~\ref{sec:gnn-edge-input-inventory}
gives the exact edge vector.}


% \subsubsection{Message-passing path}
% \label{subsec:graph-busbar-message-path}

% Asset values already sit on their busbar, so information follows the single
% path busbar $\leftrightarrow$ busbar and one message-passing layer is enough to
% carry a substation's state to an adjacent one. This is the shortest path of the
% three schemas, and the baseline against which the two disaggregated ones trade
% extra hops for individually addressable equipment.


\subsection{Disaggregated Representation}
\label{sec:heterogeneous-graph-representation}

\revision{The disaggregated graph keeps the busbars as its backbone, but gives
each generator and load a separate node. Directed or bideirectionnal edges connect these
assets to their current busbar. Transmission lines remain direct edges between
busbars.}

\begin{figure}[H]
  \centering
  \resizebox{0.92\linewidth}{!}{\input{figures/graph_heterogeneous_equipment.tex}}
  \caption{Disaggregated representation. Busbars are blue, generators
  are red, and loads are green. Physical transmission lines remain direct
  relations between busbar nodes.}
  \label{fig:heterogeneous-equipment-graph}
\end{figure}

\subsubsection{Nodes and features}
\label{subsec:heterogeneous-nodes}

\revision{The busbar nodes are the same as in
Section~\ref{subsec:graph-busbar-nodes}. The graph then adds one node for every
generator and load whose substation belongs to the local graph.}
\latestrevision{All three node types use one common feature layout so that they
can pass through the same encoder. Type indicators identify busbars,
generators, and loads; physical values occupy only the columns relevant to that
type. Disconnected assets remain indexed but their power and angle are zero.
The ordered vector and per-type inventory are given in
Appendix~\ref{sec:gnn-node-input-inventory}.}

\subsubsection{Relations and edge features}
\label{subsec:heterogeneous-relations}

% {\color{blue}Transmission lines keep the busbar-to-busbar candidates of
% Equation~\ref{eq:candidate-line-edges}; what this schema adds is the asset
% attachments. For generator $g$ at substation $s(g)$, the fixed candidate graph
% contains
% \begin{equation}
%   g \rightarrow i(s(g),b)
%   \quad\text{and}\quad
%   i(s(g),b) \rightarrow g,
%   \qquad b\in\{0,\ldots,B-1\},
%   \label{eq:generator-busbar-candidates}
% \end{equation}
% that is, $B$ candidates per direction. Loads follow the same rule.
% Appendix~\ref{app:graph-construction} collects the resulting candidate-edge
% counts.}
% \\
% \\
\latestrevision{Every directed edge uses a common layout containing a physical
line block and a one-hot relation block. Transmission-line edges populate both;
asset attachments and structural edges use a zero physical block and identify
only their type and direction. This prevents a structural relation from
looking like a measured line. The exact columns and activation rules are listed
in Appendix~\ref{sec:gnn-edge-input-inventory}.}

\noindent\revision{The direction of generator and load edges can be chosen
independently. Each asset type can use \texttt{bidirectional},
\texttt{asset\_to\_busbar}, or \texttt{busbar\_to\_asset}. The default is
bidirectional. Transmission-line and same-substation relations remain
bidirectional.}



% {\color{blue}\subsubsection{Message-passing path}
% \label{subsec:heterogeneous-architecture}

% With bidirectional asset and line relations, information follows the path
% \begin{equation}
%   \text{generator}
%   \leftrightarrow
%   \text{local busbar}
%   \leftrightarrow
%   \text{remote busbar}
%   \leftrightarrow
%   \text{load}.
%   \label{eq:heterogeneous-message-path}
% \end{equation}
% One message-passing layer crosses one arrow. Generator or load information
% therefore reaches its local busbar after one layer and the adjacent remote
% busbar after two. With one-way asset relations, the reverse flow is absent.}


\subsection{Disaggregated Line-Node Representation}
\label{sec:heterogeneous-line-graph}

\revision{The disaggregated line-node graph goes one step further. Instead of a
direct edge between two busbars, each transmission line becomes a node placed
between them. Busbars, generators, loads, and lines are therefore all explicit
nodes.}

\begin{figure}[htbp]
  \centering
  \resizebox{0.92\linewidth}{!}{\input{figures/graph_heterogeneous_line_nodes.tex}}
  \caption{Disaggregated line-node representation.
  Yellow nodes represent transmission lines and connect the blue busbars at their
  endpoints; generators and loads remain separate terminal nodes.}
  \label{fig:heterogeneous-line-node-graph}
\end{figure}

\subsubsection{Nodes and features}
\label{subsec:heterogeneous-line-nodes}

\latestrevision{All four node types again share one ordered feature layout.
Transmission-line state now occupies the line-node row, while generator and
load rows carry their own power and angle. Optional maintenance values are
included only when available. Appendix~\ref{sec:gnn-node-input-inventory}
reports the exact columns, dimensions, and zero-filled entries.}

\subsubsection{Relations and edge features}
\label{subsec:heterogeneous-line-relations}

% {\color{blue}This schema replaces the busbar-to-busbar candidates of
% Equation~\ref{eq:candidate-line-edges} by attachments to a line node. Let
% $o(\ell)$ and $e(\ell)$ denote the origin and extremity substations of line
% $\ell$. For every busbar index $b$, the fixed candidate graph contains
% \begin{equation}
%   \begin{aligned}
%     i(o(\ell),b) &\rightarrow \ell, &\qquad \ell &\rightarrow i(o(\ell),b), \\
%     i(e(\ell),b) &\rightarrow \ell, &\qquad \ell &\rightarrow i(e(\ell),b),
%   \end{aligned}
%   \qquad b\in\{0,\ldots,B-1\},
%   \label{eq:heterogeneous-line-candidates}
% \end{equation}
% that is, $2B$ candidates per end.}
% \\
% \\
% The line direction is \texttt{bidirectional}, \texttt{line\_to\_busbar}, or
% \texttt{busbar\_to\_line}; a one-way mode retains only one column of
% Equation~\ref{eq:heterogeneous-line-candidates}. An online line therefore has
% four active attachments in the bidirectional case and two in a one-way case.
% \\
% \\
\revision{The edges now describe only the type and direction of each
connection.} \latestrevision{They need no continuous line attributes because
those values already belong to the line node. Every active edge therefore
selects exactly one relation type, while inactive candidates are zero-filled
and removed before message passing. The nine relation columns and activation
rules are listed in Appendix~\ref{sec:gnn-edge-input-inventory}.}


% \noindent Origin, extremity, and direction have separate relation types. Generator and
% load attachments follow Equation~\ref{eq:generator-busbar-candidates};
% same-substation relations remain optional.

% \subsubsection{Message-passing path}
% \label{subsec:heterogeneous-line-message-path}

% The direct-line graph connects endpoint busbars in one hop. The line-node
% graph uses
% \begin{equation}
%   i_o \rightarrow \ell \rightarrow i_e,
%   \label{eq:heterogeneous-line-two-hop-path}
% \end{equation}
% so endpoint exchange requires two message-passing steps. The first step forms
% \begin{equation}
%   \mathbf{h}_{\ell}^{(1)}
%   = f_{\ell}\!\left(
%       \mathbf{h}_{\ell}^{(0)},
%       \mathbf{h}_{i_o}^{(0)},
%       \mathbf{h}_{i_e}^{(0)}
%     \right),
% \end{equation}
% and the second returns this joint state to the endpoints. Later layers enlarge
% the receptive field. The edges around the line node are bidirectional: the line gathers both endpoints and sends the updated state back; endpoint exchange is possible.

\subsection{Local actor information boundary and the global state graph}
\label{subsec:graph-local-global}

\revision{Agent $a$ controls a set of substations $\mathcal{C}_a$. A boundary
line links this region to a substation controlled by another agent. The local
graph includes only the outside information needed to represent that shared
line, and the exact amount depends on the graph representation.}

\paragraph{Why one hop, and why not more.}
\revision{I use a one-hop boundary to keep the actor local while the critic
still observes the full grid during training. Power flow is not truly limited
to one hop, so this is a practical choice rather than a claim of optimality.
The loading and angle difference of each boundary line provide the most direct
interaction with the neighboring region without revealing all of its private
asset states.}

\subsubsection{Visibility and masking}

\revision{A neighboring busbar is visible only while an active boundary line
connects it to the controlled region. If that connection disappears, the node
features are set to zero, its edges are disabled, and the node is removed from
the graph readout. The node therefore contributes neither to message passing nor to the graph
embedding. A same-substation structural edge cannot make it visible again.}
\\
\\
\revision{Controlled nodes remain visible even when they are disconnected,
because the agent may reconnect equipment to them. In the line-node graph, a
shared line node also remains present, but its attachment edges are disabled
while the line is offline. An attachment edge is active only when the line is
connected and the current busbar assignment matches that edge. Nodes for
neighboring generators and loads are never created.}

% Figure~\ref{fig:dynamic-neighbor-visibility} summarizes the dynamic visibility
% mask and the disaggregated line-node exception.

% \begin{figure}[H]
%   \centering
%   \resizebox{0.94\linewidth}{!}{%
%     \input{figures/graph_dynamic_neighbor_mask.tex}%
%   }
%   \caption{Dynamic visibility with a fixed candidate graph. In the direct-edge
%   schemas, reassignment changes the active pair and visible far-end busbar;
%   disconnection masks all contextual busbars. In
%   disaggregated line-node, the shared line node remains visible while its
%   attachments are masked. Gray dashed elements have zero masks.}
%   \label{fig:dynamic-neighbor-visibility}
% \end{figure}

\subsubsection{Representation-dependent information}

\revision{The three representations expose different views of the neighboring
region.} \latestrevision{Figure~\ref{fig:representation-dependent-neighbors}
makes this difference explicit; the corresponding inventory is retained in
Appendix Table~\ref{tab:local-information-boundary}.}

\begin{figure}[H]
  \centering
  \resizebox{\linewidth}{!}{%
    \input{figures/graph_neighbor_context.tex}%
  }
  \caption{One live boundary line, seen under each representation. In every
  panel the blue dashed region is the agent's controlled domain and the orange
  dashed region is what lies beyond it. The busbar graph~(a) reaches a far-end
  busbar carrying the neighbor's generation and load aggregated onto it. The
  disaggregated graph~(b) reaches the same busbar, but no neighboring generator
  or load node is ever constructed, so that busbar carries no measurement of
  its own. The disaggregated line-node graph~(c) reaches only the shared line
  node, and no far-end busbar exists at all.}
  \label{fig:representation-dependent-neighbors}
\end{figure}

\noindent\revision{In the busbar and disaggregated graphs, the far-end busbar is
needed because the boundary line is an edge and an edge must have two endpoints.
Only the busbar used by the active line is visible; the other candidate busbars
remain masked. If the line disconnects, no far-end busbar remains visible. In
the disaggregated graph, this neighboring busbar contains only type and domain
indicators because the power and angle measurements belong to generator and
load nodes, which are not created outside the controlled region. The boundary
line edge is therefore the only source of neighboring measurements. The
line-node graph removes the far-end busbar completely and stores the shared
measurements on the line node. When the line disconnects, the line node remains
with \texttt{line\_status}$=0$, while its attachment edges become inactive.}
\\
\\
\revision{These information limits apply only to the actors. During training,
the centralized critic still receives the normalized flat state of the full
grid. At execution time, each actor uses only its own local graph. This is the
centralized-training, decentralized-execution setting used by
MAPPO~\cite{yu2022mappo}.}

\subsection{Graph actor and centralized critic}
\label{subsec:graph-representation-encoder-boundary}

\begin{figure}[H]
  \color{black}
  \centering
  \resizebox{\textwidth}{!}{%
    \input{figures/graph_actor_pipeline.tex}%
  }
  \caption{End-to-end graph-actor architecture. Candidate edges masked by the
  current topology are removed before message passing. The graph encoder and
  its output projection are shared across agents, while the final MLP has one
  output per action available to agent $a$ and is therefore agent-specific.}
  \label{fig:graph-actor-pipeline}
\end{figure}

\revision{Figure~\ref{fig:graph-actor-pipeline} shows the complete actor. The
node features are first projected to a hidden width $d_h$. The encoder then
applies $K$ message-passing layers before reducing the node states to one graph
embedding of width $d_z$. The encoder family, depth $K$, and hidden width $d_h$
are experimental choices and are therefore not fixed in this chapter. They are
selected in Chapter~\ref{ch:experiments}. The encoder and output projection are
shared across agents, but the final action heads are separate because agents
can have different numbers of actions.}
\\
\\
\revision{The critic is a separate MLP that receives the normalized flat state.
It does not reuse the actor's graph embedding and is present only during
training. Its normalization is independent of the graph-input transformation.
The appendix records the exact encoder, graph-readout, output-projection, and
action-head equations used by the evaluated configurations.}

\section{Substation Representation: Direct Edges and Summary Nodes}
\label{sec:substation-representation}

\revision{The base graphs do not directly connect the two busbars of one
substation. I test two optional ways to add this internal communication. Factor
$e$ creates direct edges between the busbars. Factor $n$ creates a learned
summary node for the substation. In both cases, the added nodes and edges pass
through the same projection, masking, and message-passing pipeline as the rest
of the graph.}

\begin{figure}[H]
  \centering
  \resizebox{0.94\linewidth}{!}{%
    \input{figures/graph_substation_augmentations.tex}%
  }
  \caption{The four combinations of the independent substation factors $e$ and
  $n$. The hollow node $b_2$ is controlled but non-energized. Factor $e$ adds a
  bidirectional relation between the controlled busbars, while factor $n$
  connects a summary node $u_s$ to both. Neither augmentation extends to the
  contextual busbar $b_N$, which retains only its physical relation.}
  \label{fig:substation-augmentation-combinations}
\end{figure}

\noindent \latestrevision{The placement of these augmentations in all three graph
schemas, together with their exact feature values, is documented in
Appendix~\ref{app:graph-augmentation-details}.}

\subsection{Direct same-substation edges (e)}
\label{subsec:graph-relation-edges}

\revision{Factor $e$ adds the edge $i(s,b)\rightarrow i(s,b')$ for every ordered
pair of different busbars in the same controlled substation. Both directions
are present and remain active throughout the episode. These are structural
edges, not electrical lines. A controlled busbar can therefore exchange
messages with the other busbar of its substation even when it is not energized,
but it is still treated as electrically disconnected.}
\latestrevision{Every physical edge value is zero because this structural
relation carries information rather than power flow. The schema-specific
encoding is listed in Appendix
Table~\ref{tab:same-substation-augmentation-features}.}
\\
\\
\noindent\textbf{Why controlled substations only.}\revision{ The edge represents
the actor's ability to move equipment between busbars. Adding the same edge in
a neighboring substation would reveal part of another agent's action space and
would create a nonphysical communication path.}

\subsection{Substation summary nodes (n)}
\label{sec:substation-summary-nodes}

\revision{Factor $n$ adds one summary node $u_s$ for each controlled
substation. It is connected in both directions to every visible busbar of that
substation, which adds one node and $2B$ directed edges. Two busbars can then
communicate through the path
$i(s,b)\rightarrow u_s\rightarrow i(s,b')$. This serves the same purpose as
the direct edge, but takes two message-passing steps instead of one. Only
controlled busbars connect to the summary node, so it does not provide an extra
path into a neighboring region.}
\\
\\
\revision{The summary node is initialized from the structure of the substation,
not from its identity. The vector $\boldsymbol\phi_s\in\mathbb R^{4}$ contains
the numbers of busbars, connected lines, generators, and loads. The initialization is}
\begin{equation}
  u_s^{(0)} = \mathbf W_{\mathrm{sub}}\,\boldsymbol\phi_s + \mathbf b_{\mathrm{sub}},
  \qquad
  \mathbf W_{\mathrm{sub}}\in\mathbb R^{d_h\times 4},\;
  \mathbf b_{\mathrm{sub}}\in\mathbb R^{d_h}.
  \label{eq:substation-summary-init}
\end{equation}
\revision{The node starts with structural information only. Its state becomes
dependent on the current observation after it receives messages from the
busbars.}
\\
\\
\noindent\revision{The construction is the same in all three graph schemas. A mean/max graph readout counts each
$u_s$ as one node. Factors $e$ and $n$ can be enabled together, and both can be
combined with the virtual-node readout of
Section~\ref{sec:virtual-node-readout}.}

\section{\texorpdfstring{\revision{Graph Readout}}{Graph Readout}}
\label{sec:graph-pooling}

\revision{After message passing, the actor still needs one fixed-size vector for
the full local graph. This step is called graph readout. It can reduce a chosen
set of node embeddings with a mean or a maximum, or it can use the state of one
learned virtual node. The same output projection and action head are used after
either choice.}

\subsection{\texorpdfstring{\revision{Mean and maximum graph readout}}{Mean and maximum graph readout}}
\label{subsec:graph-mean-pooling}

\revision{A graph readout makes two separate choices: which nodes to include and
how to combine them. For each readout, let $\mathcal V_{\mathrm{sel}}$ denote
the selected nodes. Depending on the readout type, this set contains all
visible nodes, only controlled nodes, only energized nodes, or nodes that are
both controlled and energized. A node is energized only if it touches an active
electrical relation. The actor then applies either a mean or a component-wise
maximum to the embeddings in $\mathcal V_{\mathrm{sel}}$:}
\begin{equation}
  \overline{\mathbf{h}}^{\,\mathrm{mean}}_G =
    \frac{1}{|\mathcal V_{\mathrm{sel}}|}
    \sum_{v\in\mathcal V_{\mathrm{sel}}}\mathbf{h}^{(K)}_v,
  \qquad
  \left(\overline{\mathbf{h}}^{\,\max}_G\right)_c =
    \max_{v\in\mathcal V_{\mathrm{sel}}}
    \left(\mathbf{h}^{(K)}_v\right)_c .
  \label{eq:methods-pooling}
\end{equation}
\revision{The readout combines all selected node types together;
it does not create a separate graph vector for each type. A node outside
$\mathcal V_{\mathrm{sel}}$ does not contribute to the readout. Combining the
four possible node populations with the two reducers gives the eight readouts
in Table~\ref{tab:pooling-readout-names}.}

\begin{table}[H]
  \centering
  \small
  \caption{The eight final \revision{graph readouts}, named
  \texttt{[controlled\_][energized\_]\{mean,max\}}. Rows choose the \revision{readout}
  population, columns the reducer. The virtual-node readout of
  Section~\ref{sec:virtual-node-readout} replaces the \revision{selected-node readout} entirely.}
  \label{tab:pooling-readout-names}
  \setlength{\tabcolsep}{6pt}
  \begin{tabular}{L{0.30\textwidth}ll}
    \toprule
    \revision{Selected node population} & \texttt{mean} & \texttt{max} \\
    \midrule
    \revision{All visible nodes} & \texttt{mean} & \texttt{max} \\
    \revision{Controlled nodes} & \texttt{controlled\_mean} & \texttt{controlled\_max} \\
    \revision{Energized nodes} & \texttt{energized\_mean} & \texttt{energized\_max} \\
    \revision{Controlled and energized nodes} & \texttt{controlled\_energized\_mean} & \texttt{controlled\_energized\_max} \\
    \bottomrule
  \end{tabular}
\end{table}

\noindent\revision{The selected population changes only the final graph
readout. It does not remove nodes from message passing. A visible contextual
node can still send information to controlled nodes even when it is not part of
$\mathcal V_{\mathrm{sel}}$. Screen C therefore compares graph readouts, not
different information boundaries.}

{\color{black}
\paragraph{What each axis trades.}
\revision{The node scope and the reducer answer different questions, so their
trade-offs should be considered separately
(Table~\ref{tab:pooling-design-comparison}). Before running Screen C, the safest
default was \texttt{controlled\_mean}: the node population is stable, it matches
the actor's region, and it keeps empty busbars that may receive equipment.
\texttt{controlled\_max} is a more risk-sensitive choice when one critical node
should dominate the decision.}

\begin{table}[H]
  \color{black}
  \centering
  \small
  \caption{Trade-offs along each axis of Table~\ref{tab:pooling-readout-names}.}
  \label{tab:pooling-design-comparison}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{L{0.18\textwidth}L{0.34\textwidth}L{0.38\textwidth}}
    \toprule
    Choice & Main advantage & Main limitation \\
    \midrule
    \multicolumn{3}{l}{\textit{Reducer}} \\
    \texttt{mean}
      & Smooth regional summary; gradients reach every pooled node.
      & A rare but critical activation is diluted. \\
    \texttt{max}
      & Keeps the strongest activation and ignores how many nodes there are.
      & Sparse gradients, and channel maxima may come from unrelated nodes. \\
    \midrule
    \multicolumn{3}{l}{\textit{Pooled population}} \\
    All visible
      & Boundary conditions enter the readout directly.
      & Context can dilute a mean or dominate a max, and the population moves with boundary topology. \\
    Controlled
      & Aligned with the action domain and fixed for an episode; context still arrives through message passing.
      & Relies on message passing to carry every useful boundary signal. \\
    Energized
      & Focuses on the network currently carrying power.
      & Drops available action targets, and the population changes after each switching action. \\
    \bottomrule
  \end{tabular}
\end{table}
}

\subsection{Virtual-node graph readout}
\label{sec:virtual-node-readout}

\revision{The virtual-node option replaces the selected-node readout with one
learned summary node $v_{\star}$. It can be used with any of the three graph
representations. When substation-summary nodes are enabled, each controlled
summary node sends one edge to $v_{\star}$. Otherwise, every visible controlled
busbar sends one edge to it. In all reported runs, these edges point only toward
the virtual node. It can collect information but cannot send a message back into
the physical graph. Contextual busbars, generators, loads, and line nodes do not
connect to it directly; their information must first reach the controlled
region through normal message passing.}
\\
\\
\revision{The initial state $\mathbf{h}_{\star}^{(0)}$ is learned. The same
message-passing layers update the virtual node and the physical nodes. After
$K$ layers, $\mathbf{h}_{\star}^{(K)}$ replaces the usual graph-readout vector
before the output projection, so the embedding size does not change. The added
edges are structural. The graph receives one such edge per visible controlled
busbar, or one per controlled substation when summary nodes are used.}

\begin{figure}[H]
  \centering
  \resizebox{0.90\linewidth}{!}{%
    \input{figures/graph_virtual_readout.tex}%
  }
  \caption{Virtual-node readout in the reported one-way setting. Without
  substation nodes, every controlled busbar, including non-energized
  $b_{32}$, sends directly to $v_\star$. With augmentation $n$, the same
  busbars communicate through $u_s$, and only the substation nodes send to
  $v_\star$. The final state
  $\mathbf h_\star^{(K)}$ replaces the \revision{selected-node readout}.}
  \label{fig:virtual-node-readout}
\end{figure}

\section{Candidate-Action Scoring from the Disaggregated Line-Node Graph}
\label{sec:candidate-action-scoring}

\revision{The default action head has one output for every discrete action.
Each output is tied to an index, so the network must learn separately what each
index means. It does not know which physical objects the action changes. The
alternative head in this section scores an action from the graph nodes that it
touches. This option is available only for the disaggregated line-node graph of
Section~\ref{sec:heterogeneous-line-graph}. The PPO loss, categorical policy,
sampling procedure, and deterministic evaluation remain unchanged.}

\subsection{From action indices to candidate scoring}
\label{subsec:candidate-motivation}

\revision{Let $\mathbf{z}_a$ be the $d_z$-dimensional graph embedding of agent
$a$. It is obtained from the local node states by the graph readout of
Section~\ref{sec:graph-pooling}, followed by a learned output projection. The
default action head receives only this single vector.}
\\
\\
\revision{With the default head, the logits are produced directly from
$\mathbf{z}_a$:}
\begin{equation}
  \boldsymbol{\ell}_a = \operatorname{MLP}_a(\mathbf{z}_a),
  \qquad \boldsymbol{\ell}_a\in\mathbb{R}^{|\mathcal{A}_a|}.
  \label{eq:action-index-head}
\end{equation}
\revision{The final layer has one row for each action index. The model must
therefore learn each action separately, and the head becomes wider when the
action space $|\mathcal{A}_a|$ grows.}
\\
\\
\revision{Candidate scoring replaces these independent output rows with one
scalar-valued network shared by all non-idle actions. The idle action can use
the same scorer or a separate one:}
\begin{equation}
  \ell_{a,j} =
  \operatorname{score}
  \left(
    \underbrace{\mathbf{z}_a}_{\text{whole graph}},\
    \underbrace{\mathbf{c}_j}_{\text{nodes action }j\text{ touches}},\
    \underbrace{\boldsymbol{\phi}_j}_{\text{optional action descriptor}}
  \right),
  \qquad j\geq 1.
  \label{eq:candidate-scoring-head}
\end{equation}
\revision{The same parameters now score every non-idle action, so the number of
learned scorer parameters no longer grows with $|\mathcal{A}_a|$. Two actions
are compared through the graph nodes they affect instead of through unrelated
output weights.}
\\
\\
\revision{The candidate head has three independent design choices. First, how the touched
nodes are combined into an action context $\mathbf{c}_j$. Second, a
static action descriptor $\boldsymbol\phi_j$ may be appended. Third, the idle
action may share the scorer or use its own head. Combining together these 3 options gives eight variants. The complete structure to score one action is
shown in Figure~\ref{fig:candidate-action-scoring-pipeline}.}

\begin{figure}[H]
  \centering
  \resizebox{0.99\linewidth}{!}{%
    \input{figures/candidate_action_scoring_pipeline.tex}%
  }
  \caption[Candidate-action scoring pipeline]{Candidate-action scoring for an
  endpoint-move candidate. Stage~1 is the shared GNN encoder, given in full in
  Figure~\ref{fig:graph-actor-pipeline}: it maps the local graph to node states
  $\mathbf H$. The same stack gives the global embedding $\mathbf z_a$ by
  \revision{selected-node readout} and projection. In stage~2 the violet outlines mark the only
  rows gathered from $\mathbf H$ for the local context: both busbars of the
  modified substation and the touched line node. One \revision{candidate-pooling}
  construction then produces $\mathbf c_j$. The static descriptor
  $\boldsymbol\phi_j$ may be appended before the shared non-idle scorer. The
  idle action uses either zero local context in that scorer or a dedicated
  scorer.}
  \label{fig:candidate-action-scoring-pipeline}
\end{figure}

\subsection{What each action touches}
\label{subsec:candidate-action-metadata}

\revision{The scorer first needs to know which graph nodes belong to each
action. I build one lookup table per agent, with one row for every action. The
table is created once with the environment and is not recomputed during
training.}
\\
\\
\latestrevision{A Grid2Op topology action identifies the line endpoints,
generators, loads, and substations that it changes. These objects are mapped
once to their graph rows. All busbars of a modified substation are included so
that the destination context is represented, while a boundary action never
adds private nodes from another region. The row-mapping example, padding and mask
rules, and complete descriptor are reported in
Appendix~\ref{app:candidate-scorer-details}.}

\subsection{\texorpdfstring{\revision{Candidate pooling of the touched nodes}}{Candidate pooling of the touched nodes}}
\label{subsec:candidate-pooling}

\latestrevision{To score action \(j\), the model needs the embeddings of the
graph nodes affected by that action. The encoder therefore returns both the
global graph embedding \(\mathbf z_a\) and a matrix \(\mathbf H\) containing
one embedding for each node:}
\begin{equation}
  (\mathbf{z}_a,\mathbf{H})
  = \operatorname{encoder}_{\mathrm{nodes}}(\mathcal{G}^{(a)}_t),
  \qquad
  \mathbf{H}\in\mathbb{R}^{|\mathcal{V}|\times d_h},
  \label{eq:forward-with-nodes}
\end{equation}
\latestrevision{Row \(v\) of \(\mathbf H\) is the final embedding
\(\mathbf{h}^{(K)}_v\) of node \(v\). The rows associated with action \(j\) are
selected from this matrix and then combined to form its context
\(\mathbf c_j\).}
\\
\\
Write $\mathcal{N}_t(j)$ for the rows of type
$t\in\{\mathrm{bus},\mathrm{line},\mathrm{load},\mathrm{gen}\}$ that action $j$
touches. The masked mean over any such set is
\begin{equation}
  \mathbf{c}^{(t)}_j =
  \frac{1}{\max\left(1,|\mathcal{N}_t(j)|\right)}
  \sum_{v\in\mathcal{N}_t(j)}\mathbf{h}^{(K)}_v,
  \label{eq:candidate-typed-mean}
\end{equation}
\revision{The denominator is clamped so that an action touching no node of type
$t$ produces a zero vector instead of a division by zero. Joint-mean pooling
applies the equation once to all touched nodes and produces a vector of width
$d_h$. Typed-mean pooling computes one mean for each node type and concatenates
the four results. Its width is $4d_h$, and it keeps the physical roles separate
(Table~\ref{tab:candidate-pool-modes}).}

\begin{table}[H]
  \centering
  \small
  \caption{Implemented \revision{candidate-pooling} constructions for the action-specific node context.}
  \label{tab:candidate-pool-modes}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{L{0.22\textwidth}L{0.28\textwidth}L{0.30\textwidth}L{0.10\textwidth}}
    \toprule
    Construction & Eligible rows & Aggregation & Width \\
    \midrule
    Joint uniform mean
      & All nodes touched by the action
      & One equal-weight mean; node roles are mixed
      & $d_h$ \\\addlinespace[2pt]
    Type-separated uniform mean
      & All nodes touched by the action
      & One equal-weight mean per type, then concatenation
      & $4d_h$ \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Scoring and the idle action}
\label{subsec:candidate-scorer}

\latestrevision{One scalar-output network scores every non-idle action from the
global graph embedding, the candidate context, and the optional static
descriptor. For a non-idle action, the scorer input and logit are}
{\color{black}
\begin{equation}
  \mathbf{x}_{a,j} =
  \begin{cases}
    [\mathbf{z}_a;\mathbf{c}_j;\boldsymbol{\phi}_j],
      & \text{with the descriptor},\\
    [\mathbf{z}_a;\mathbf{c}_j],
      & \text{without it},
  \end{cases}
  \qquad
  \ell_{a,j}=\operatorname{MLP}_{\mathrm{score}}(\mathbf{x}_{a,j}),
  \quad j\geq 1.
  \label{eq:candidate-shared-scorer}
\end{equation}
}
\latestrevision{The idle action either reuses this scorer with an empty
candidate context or uses a dedicated head, which treats do nothing as a
separate global decision:}
{\color{black}
\begin{equation}
  \ell_{a,0} =
  \begin{cases}
    \operatorname{MLP}_{\mathrm{score}}(\mathbf{x}_{a,0}) + b_{\varnothing},
      & \text{shared, with }\mathbf{c}_0=\mathbf{0},\\
    \operatorname{MLP}_{\varnothing}(\mathbf{z}_a) + b_{\varnothing},
      & \text{dedicated.}
  \end{cases}
  \label{eq:candidate-idle-scorer}
\end{equation}
}
\latestrevision{Here, \(b_{\varnothing}\) is a learned scalar offset that
calibrates the do-nothing logit relative to the non-idle action logits. It is
applied whether action \(0\) reuses the shared MLP scorer or uses a dedicated
MLP head.}

\subsection{Global-maximum and action-delta extension}
\label{subsec:candidate-gmax-delta}

\revision{The Gmax-delta variant keeps the same shared scorer but changes the
information given to it. First, the global vector $\mathbf z_a$ uses a
component-wise maximum over energized nodes instead of a mean. Second, every
exact modification in action $j$ is encoded from the object being changed, the
mean embedding of its substation busbars, the target busbar, the difference
between target and source context, and static operation flags. These tokens are
combined into $\boldsymbol\delta_j$ and appended to the scorer input.} \latestrevision{The complete branch diagram is
shown in Figure~\ref{fig:candidate-action-gmax-delta-pipeline}.}

\begin{figure}[H]
  \color{black}
  \captionsetup{labelfont={color=black},textfont={color=black}}
  \centering
  \resizebox{0.99\linewidth}{!}{%
    \input{figures/candidate_action_gmax_delta_pipeline.tex}%
  }
  \caption[Gmax-delta candidate-action architecture]{Gmax-delta extension of
  Figure~\ref{fig:candidate-action-scoring-pipeline}. The shared graph encoder
  returns node states $\mathbf H$. The upper branch builds the global vector by
  maximum graph readout over energized nodes; the middle branch keeps the
  touched-node context $\mathbf c_j$; and the lower branch encodes each exact
  object modification into $\boldsymbol\delta_j$.}
  \label{fig:candidate-action-gmax-delta-pipeline}
\end{figure}

\section{Cross-Grid Input Transformations}
\label{subsec:graph-feature-normalization}

\revision{The graph architecture can be reused on grids of different sizes,
but the numerical inputs can still change from one grid to another. The same
physical quantity may have a different range, and voltage angles depend on the
arbitrary reference bus. I therefore test two optional input transformations:
fixed physical scaling and a reference-independent angle representation. They
change the values given to the encoder, but not the encoder itself.}

\subsection{Physical scaling}

\revision{For every continuous feature $f$, the scaled value is
$x'_f=x_f/s_f$, where $s_f$ is a fixed reference in the same unit. This keeps
the meaning of the feature and, importantly, keeps zero equal to zero. An empty
busbar therefore has zero-valued asset features on every grid. Power is divided
by the rating of the largest installed generator,
$s_P=\max_g P_g^{\max}$, while angles are divided by $180$ degrees. Cooldown,
overflow, and maintenance counters are divided by the limits defined by
Grid2Op and listed in Table~\ref{tab:graph-physical-scales}. These scales are
fixed when the environment is created; they are not estimated from training
data. The loading ratio \texttt{rho}, binary indicators, masks, node types, and
relation types are already dimensionless and are left unchanged.}

\subsection{Angle representation}
\label{sec:line-node-angle}

\revision{An absolute voltage angle needs an arbitrary zero, usually defined by
the slack bus. If the same constant is added to every bus angle, the physical
state does not change. The difference between the two ends of a line avoids
this problem:}
\[
  \Delta\theta_{\ell} \;=\; \theta^{\,\mathrm{or}}_{\ell} - \theta^{\,\mathrm{ex}}_{\ell}.
\]
\revision{This line-angle difference does not depend on the reference bus. It
is also available for every observed line, whereas an absolute angle is stored
only where a generator or load is connected. When this option is enabled, the
absolute-angle channels are removed. Chapter~\ref{ch:encoder-inputs} shows why
this matters: the two grids differ by about $5$ to $7^{\circ}$ in absolute
angle, mostly because they use different slack-bus references. Dividing by a
fixed scale cannot remove that offset.}

% \subsection{Use across the experiments}

% \revision{The sparse-control and architecture-screening experiments use raw
% features and absolute angles. The transfer study later compares this original
% setting with physical scaling and line-angle differences enabled together. The
% comparison is made on both grids so that their effect can be separated from the
% change of environment. Running standardization was also implemented, but no
% reported policy uses it. The offline analysis in
% Section~\ref{sec:wcci-running-norm} shows that it increases the mismatch between
% the two grids.}

\section{Action-Space Reduction for Large Grids}
\label{sec:wcci-action-space-reduction}

\revision{The graph representation changes what an agent sees. Action-space
reduction changes which decisions it can make. This step is necessary on WCCI,
where the full discrete topology-action space is much larger than on
\texttt{bus14}. Before MAPPO training, actions are ranked offline using
simulated outcomes. States are collected during rollouts whenever the grid is
stressed:}
\[
  \rho_{\max}>0.95.
\]
\revision{For every collected state $s$, candidate actions are simulated for
one step and compared with doing nothing:}
\[
  \Delta(s,a)=\rho_{\mathrm{after}}(s,a)
  -\rho_{\mathrm{after}}(s,0).
\]
\revision{An action is useful when it reduces the post-action loading by more
than $\epsilon$, which means $\Delta(s,a)<-\epsilon$. Its score is the fraction
of evaluated states in which it improves on do nothing:}
\[
  \mathrm{score}(a)=
  \frac{\#\{\text{tested states where }a\text{ improves the grid}\}}
       {\#\{\text{tested states where }a\text{ is evaluated}\}},
\]
\revision{The reduced space keeps the $k$
highest-scoring actions for each agent and always keeps action 0. If an agent
has fewer than $k$ actions, its complete action set is retained.}
\\
\\
\revision{The value of $k$ is chosen empirically. I first evaluate each reduced
set with a one-step greedy controller. At every risky state, it simulates the
available actions and executes the one that improves loading the most. This is
not a learned policy; it checks how much useful control remains after the action
space has been cut. Section~\ref{sec:wcci-environment} reports the resulting
action counts and the values of $k$ used in the transfer experiments.}
\\
\\
\begin{tcolorbox}[
  colframe=red!75!black,
  colback=red!5!white,
  title={Chapter Summary}
]
\color{black}
\textbf{Purpose:} Define a graph actor whose representation, graph-level
summary, and action interface can be evaluated separately and reused across
grids.

\begin{itemize}
  \item \textbf{Graph construction:} The three schemas decide which physical
  objects receive nodes and what one-hop boundary information is visible to a
  decentralized actor. The centralized critic still observes the full state.
  \item \textbf{Augmentation and graph readout:} Factors $e$ and $n$ change
  message passing inside controlled substations. Mean, maximum, selected-node,
  and virtual readouts determine how the encoded graph becomes one vector.
  \item \textbf{Action interface:} The default head learns one output per
  action index. Candidate scoring instead shares a scorer across actions and
  conditions each score on the nodes changed by that action.
  \item \textbf{Transfer preparation:} Fixed physical scales and line-angle
  differences improve input compatibility, while offline action ranking keeps
  a manageable WCCI action set whose coverage is checked by a greedy simulator.
\end{itemize}

\textbf{Takeaway:} Graph representation, information boundary, readout,
action scoring, input transformation, and action-space size are separate design
choices. This chapter defines the choices needed to interpret the experiments;
their exact tensor layouts and implementation inventories are retained in the
appendices.
\end{tcolorbox}
