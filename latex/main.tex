\documentclass[10pt,a4paper]{report}

\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{setspace}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{array}
\usepackage{graphicx}
\usepackage{xcolor}
\definecolor{darkred}{rgb}{0.55,0.05,0.05}
% Clarifications added in answer to review questions. Distinct from the
% blue/red/green/orange review markup and from darkred.
\definecolor{clarif}{rgb}{0.00,0.42,0.42}
\usepackage{subcaption}
\usepackage{pdflscape}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{float}
\usepackage{soul}
\usepackage{tcolorbox}
\usepackage[backend=biber,style=ieee]{biblatex}

\addbibresource{references.bib}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue,
  pdftitle={Thesis Plan},
  pdfauthor={Corentin Plumet}
}

% More air around headings so section boundaries are easy to spot when
% skimming. Values are in units of \baselineskip: {left}{before}{after}.
\usepackage{titlesec}
\titlespacing*{\section}      {0pt}{2.6\baselineskip}{1.0\baselineskip}
\titlespacing*{\subsection}   {0pt}{2.0\baselineskip}{0.7\baselineskip}
\titlespacing*{\subsubsection}{0pt}{1.6\baselineskip}{0.5\baselineskip}

\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

% Placeholder for a figure whose runs are still training. Replace the whole
% \pendingplot{...} call with \includegraphics when the plot exists.
\newcommand{\pendingplot}[2][0.20\textheight]{%
  \setlength{\fboxrule}{0.7pt}%
  \fbox{\begin{minipage}[c][#1][c]{0.90\linewidth}%
    \centering\small\itshape\color{gray}#2%
  \end{minipage}}%
}
\newcommand{\pending}[1]{{\color{gray}\itshape[pending: #1]}}
\setlist[itemize]{topsep=0.25em,itemsep=0.15em}
\setlist[enumerate]{topsep=0.25em,itemsep=0.15em}

\title{Thesis Plan\\\large Decentralized Multi-Agent Reinforcement Learning for Power Grid Topology Control}
\author{Corentin Plumet}
\date{\today}

\begin{document}

\pagenumbering{roman}
\hypersetup{pageanchor=false}
\maketitle
\hypersetup{pageanchor=true}
\tableofcontents
\clearpage

\pagenumbering{arabic}
\input{chapters/00_plan}
\input{chapters/01_introduction}
\input{chapters/02_baseline}
\input{chapters/04_methods}
\input{chapters/05_experiments}
\input{chapters/06_sparse_control}
\input{chapters/07_wcci_transfer}

\appendix
\chapter{GINE and GAT Graph-Actor Architectures}
\label{app:gine-architecture}

This appendix specifies the complete edge-aware actor used in the graph
experiments. GINE and GAT receive the same graph inputs and use the same
pooling and action head. Their only architectural difference is the
message-passing operator: GINE inserts an edge embedding into the message
content, whereas GAT uses an edge embedding to decide how strongly the message
is weighted.

\section{Shared input and encoder pipeline}

For agent $a$, the encoder receives the local graph
$\mathcal{G}^{(a)}_t$. Candidate edges with
\texttt{edge\_mask}$=0$ are removed before message passing, so the layer sees
only the active directed set $\mathcal{E}^{(a)}_t$. The source of
$u\rightarrow v$ sends a message and $v$ aggregates it. Node and edge masks do
not become learned features.

Each raw node vector $\mathbf{x}_v$ first
passes through
\begin{equation}
  \mathbf{h}^{(0)}_v =
  \operatorname{LayerNorm}\!\left(
    \operatorname{ReLU}(\mathbf{W}_{x}\mathbf{x}_v+\mathbf{b}_{x})
  \right),
  \label{eq:graph-node-pre-encoder}
\end{equation}
which produces a 128-dimensional node state. Learned global node-identifier
embeddings and the optional external edge pre-encoder are disabled. Every GINE
or GAT layer nevertheless contains its own trainable projection of the edge
vector to the dimension required by that layer.

Two message-passing layers are used. After each layer, the implementation
applies ReLU followed by LayerNorm:
\begin{equation}
  \mathbf{h}^{(k+1)}_v =
  \operatorname{LayerNorm}\!\left(
    \operatorname{ReLU}(\widetilde{\mathbf{h}}^{(k+1)}_v)
  \right).
  \label{eq:graph-post-convolution}
\end{equation}
The same encoder parameters are shared by all agents. Each agent nevertheless
passes its own local graph, with its own node set, active edges, and one-hop
context, through that shared encoder.

\section{GINE message passing}
\label{sec:gine-message-passing}

For an active edge $u\rightarrow v$, layer $k$ projects the complete edge
feature vector and adds it to the sender state:
\begin{align}
  \widetilde{\mathbf{e}}^{(k)}_{uv}
    &= \mathbf{W}^{(k)}_e\mathbf{e}_{uv}+\mathbf{b}^{(k)}_e, \\
  \mathbf{m}^{(k)}_{u\rightarrow v}
    &= \operatorname{ReLU}\!\left(
      \mathbf{h}^{(k)}_u+\widetilde{\mathbf{e}}^{(k)}_{uv}
    \right), \\
  \widetilde{\mathbf{h}}^{(k+1)}_v
    &= \operatorname{MLP}^{(k)}\!\left(
      (1+\epsilon)\mathbf{h}^{(k)}_v
      +\sum_{u:(u,v)\in\mathcal{E}_t}
       \mathbf{m}^{(k)}_{u\rightarrow v}
    \right).
  \label{eq:gine-layer}
\end{align}
The implementation uses the PyTorch Geometric default $\epsilon=0$ without a
trainable epsilon parameter. Each internal GINE MLP is
Linear--ReLU--Linear and outputs 128 features. Equation~\ref{eq:gine-layer}
shows why edge attributes are part of the message content. Two identical node
states can send different messages when one edge is an overloaded physical
line and the other is an asset-attachment relation. Likewise, reversing a
typed heterogeneous edge activates a different relation-one-hot column and can
produce a different message.

GINE uses an unweighted sum over incoming active edges. It does not learn a
softmax competition between neighbors. The edge embedding changes what is
sent; the number of incoming messages and their vector sum determine the
aggregate.

\section{GAT message passing}
\label{sec:gat-message-passing}

GAT first computes a score for every active incoming edge and attention head.
For head $r$, the implementation has the form
\begin{align}
  s^{(k,r)}_{uv}
    &= \operatorname{LeakyReLU}\!\left(
      \mathbf{a}_{s,r}^{\top}\mathbf{W}_{s,r}\mathbf{h}^{(k)}_u
      +\mathbf{a}_{d,r}^{\top}\mathbf{W}_{d,r}\mathbf{h}^{(k)}_v
      +\mathbf{a}_{e,r}^{\top}\mathbf{W}_{e,r}\mathbf{e}_{uv}
    \right), \\
  \alpha^{(k,r)}_{uv}
    &= \frac{\exp s^{(k,r)}_{uv}}
      {\sum_{w:(w,v)\in\mathcal{E}_t}\exp s^{(k,r)}_{wv}},
  \label{eq:gat-attention}\\
  \widetilde{\mathbf{h}}^{(k+1)}_v
    &= \frac{1}{H}\sum_{r=1}^{H}
       \sum_{u:(u,v)\in\mathcal{E}_t}
       \alpha^{(k,r)}_{uv}\mathbf{W}_{r}\mathbf{h}^{(k)}_u.
  \label{eq:gat-layer}
\end{align}
GAT uses $H=4$ heads and
\texttt{concat=false}, so the four head outputs are averaged and the layer
still returns 128 features. The edge embedding affects
$\alpha_{uv}$: it changes how much attention the receiver assigns to the
sender. In the standard GAT operator used here, the edge vector is not added to
the value vector $\mathbf{W}_{r}\mathbf{h}_u$. This is the central difference
from GINE.

GAT internally adds a self-loop for every node. That computational self-loop
is separate from the optional explicit \texttt{relation\_self} edge in the
graph schema.

\begin{table}[H]
  \centering
  \small
  \caption{Exact architectural difference between the implemented GINE and
  GAT actors.}
  \label{tab:gine-gat-comparison}
  \setlength{\tabcolsep}{4pt}
  \begin{tabular}{L{0.23\textwidth}L{0.33\textwidth}L{0.33\textwidth}}
    \toprule
    Component & GINE & GAT \\
    \midrule
    Edge use & Added to the sender state inside every message & Enters the attention score for every incoming edge \\
    Neighbor aggregation & Unweighted sum & Softmax-weighted sum \\
    Self information & Explicit $(1+\epsilon)\mathbf{h}_v$ term & Internal GAT self-loop \\
    Multiple heads & Not used & Four heads, averaged rather than concatenated \\
    Post-layer operation & ReLU, then LayerNorm & ReLU, then LayerNorm \\
    Pooling and action head & Shared implementation & Shared implementation \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Pooling and graph output}
\label{sec:gnn-pooling-action}

With mean pooling, the graph representation after the second message-passing
layer is
\begin{equation}
  \overline{\mathbf{h}}_G =
  \frac{\sum_v m_v\mathbf{h}^{(K)}_v}
       {\max(1,\sum_v m_v)},
  \label{eq:gnn-mean-pooling}
\end{equation}
where $m_v$ is the dynamic visibility mask. This is one mean over all visible
node types, not a separate mean per type. Therefore:
\begin{itemize}
  \item the busbar graph pools controlled busbars and only the far-end busbars
  currently attached by live boundary lines;
  \item the direct heterogeneous graph additionally pools controlled generators
  and loads, but never neighboring private assets;
  \item the explicit-line graph additionally pools transmission-line nodes and
  contains no neighboring equipment.
\end{itemize}
An inactive contextual candidate contributes neither to the numerator nor to
the denominator. The ordinary GINE/GAT mean readout includes visible context;
\texttt{controlled\_mean} replaces $m_v$ by the product of the visibility and
controlled-node masks. Visible context can still influence controlled nodes
through message passing, but it is not aggregated directly. Sum and maximum
pooling, including their \texttt{controlled\_*} variants, change only the
readout and do not change message passing or the information boundary.

The pooled 128-dimensional vector then passes through the shared graph output
projection
\begin{equation}
  \mathbf{z}_a =
  \operatorname{ReLU}(\mathbf{W}_{o}\overline{\mathbf{h}}_G+\mathbf{b}_{o}),
  \qquad \mathbf{z}_a\in\mathbb{R}^{128}.
  \label{eq:gnn-output-projection}
\end{equation}
When virtual-node readout is selected, the final virtual-node state replaces
$\overline{\mathbf{h}}_G$; the output projection and its dimension stay the
same.

\section{Agent-specific action head}
\label{sec:gnn-action-head}

The flat observation is not concatenated to $\mathbf{z}_a$. Each agent has its
own two-layer MLP action head:
\begin{align}
  \mathbf{q}^{(1)}_a &=
    \operatorname{ReLU}(\mathbf{W}^{(1)}_a\mathbf{z}_a+\mathbf{b}^{(1)}_a), \\
  \mathbf{q}^{(2)}_a &=
    \operatorname{ReLU}(\mathbf{W}^{(2)}_a\mathbf{q}^{(1)}_a+\mathbf{b}^{(2)}_a), \\
  \boldsymbol{\ell}_a &=
    \mathbf{W}^{(3)}_a\mathbf{q}^{(2)}_a+\mathbf{b}^{(3)}_a,
  \qquad \boldsymbol{\ell}_a\in\mathbb{R}^{|\mathcal{A}_a|}, \\
  \pi_a(\cdot\mid\mathcal{G}^{(a)}_t)
    &= \operatorname{Categorical}(\text{logits}=\boldsymbol{\ell}_a).
  \label{eq:gnn-action-head}
\end{align}
Both hidden layers contain 128 units. During training, an action is sampled
from this categorical distribution. Deterministic evaluation selects
$\arg\max_j\ell_{a,j}$. Action zero is the local do-nothing action.

Only the GNN encoder and graph output projection are shared across agents. The
action heads are agent-specific because agents can have different local action
sets and therefore different output widths $|\mathcal{A}_a|$. The critic is a
separate MLP over the centralized flat state and does not reuse the actor
GINE/GAT embedding.

Equation~\ref{eq:gnn-action-head} is the default head. The optional
candidate-action head of Section~\ref{sec:candidate-action-scoring} keeps this
pipeline up to $\mathbf{z}_a$ and additionally consumes the
post-message-passing node states $\mathbf{h}^{(K)}_v$, which are the same
states that Equation~\ref{eq:gnn-mean-pooling} averages.

\section{Exact node inputs by representation}
\label{sec:gnn-node-input-inventory}

All node types of one representation receive the same feature columns. Columns
not listed for a node type are zero. Type identity reaches GINE and GAT through
the \texttt{is\_*} columns. The inventory below describes visible nodes. For an
inactive contextual candidate, the complete row---including type and role
columns---is zero, all incident edges are inactive, and the node is excluded
from readout.

\begin{table}[H]
  \centering
  \scriptsize
  \caption{Node types and nonzero input features received by GINE and GAT,
  under the default \texttt{gnn\_angle\_representation = node}. Under
  \texttt{edge\_diff} the \texttt{gen\_theta}, \texttt{load\_theta} and
  \texttt{theta} entries leave the node rows in every representation; the
  busbar and disaggregated graphs move the angle drop onto the physical line
  edge, while the explicit-line graph writes it on the transmission-line row
  as \texttt{theta\_diff} (Section~\ref{sec:line-node-angle}).}
  \label{tab:gine-gat-node-inventory}
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{L{0.18\textwidth}L{0.17\textwidth}L{0.56\textwidth}}
    \toprule
    Representation & Node type & Populated input features \\
    \midrule
    \texttt{bus} & Busbar & \texttt{gen\_p}, \texttt{gen\_theta}, \texttt{load\_p}, \texttt{load\_theta}, \texttt{time\_before\_cooldown\_sub}, \texttt{domain\_mask} \\
    \midrule
    \texttt{heterogeneous} & Busbar & \texttt{is\_busbar}, \texttt{time\_before\_cooldown\_sub}, \texttt{connected}, \texttt{domain\_mask} \\
     & Generator & \texttt{is\_generator}, \texttt{p}=\texttt{gen\_p}, \texttt{theta}=\texttt{gen\_theta}, \texttt{connected}, \texttt{domain\_mask} \\
     & Load & \texttt{is\_load}, \texttt{p}=\texttt{load\_p}, \texttt{theta}=\texttt{load\_theta}, \texttt{connected}, \texttt{domain\_mask} \\
    \midrule
    \texttt{heterogeneous\_line} & Busbar & \texttt{is\_busbar}, \texttt{time\_before\_cooldown\_sub}, \texttt{connected}, \texttt{domain\_mask} \\
     & Generator & \texttt{is\_generator}, \texttt{p}=\texttt{gen\_p}, \texttt{theta}=\texttt{gen\_theta}, \texttt{connected}, \texttt{domain\_mask} \\
     & Load & \texttt{is\_load}, \texttt{p}=\texttt{load\_p}, \texttt{theta}=\texttt{load\_theta}, \texttt{connected}, \texttt{domain\_mask} \\
     & Transmission line & \texttt{is\_transmission\_line}, \texttt{line\_status}, \texttt{rho}, \texttt{timestep\_overflow}, \texttt{time\_before\_cooldown\_line}, optional maintenance times, \texttt{connected}, \texttt{domain\_mask} \\
    \midrule
    Any augmented schema & Substation summary & Learned projection of the substation structural descriptor; no raw physical feature row \\
     & Virtual node & One learned vector shared across graphs; no raw physical feature row \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Exact edge inputs by representation}
\label{sec:gnn-edge-input-inventory}

The edge vector contains all edge information consumed by GINE and GAT.
Inactive candidates are filtered out before the vectors in
Table~\ref{tab:gine-gat-edge-inventory} reach a convolution.

\begin{table}[H]
  \centering
  \scriptsize
  \caption{Directed edge relations and edge-feature vectors received by GINE
  and GAT, under the default \texttt{gnn\_angle\_representation = node}.
  ``Relation'' denotes one active relation-one-hot column. Under
  \texttt{edge\_diff} the physical-line block of the busbar and disaggregated
  graphs gains a \texttt{theta\_diff} column; the explicit-line rows are
  unchanged, because there the drop is a line-node feature and the attachment
  edges stay free of physical channels.}
  \label{tab:gine-gat-edge-inventory}
  \setlength{\tabcolsep}{3pt}
  \begin{tabular}{L{0.18\textwidth}L{0.28\textwidth}L{0.45\textwidth}}
    \toprule
    Representation & Directed relation & Edge features \\
    \midrule
    \texttt{bus} & Physical busbar $\leftrightarrow$ busbar & \texttt{line\_status}, \texttt{rho}, \texttt{timestep\_overflow}, \texttt{time\_before\_cooldown\_line}, optional maintenance times; no relation one-hot \\
     & Optional self or same-substation & Same width; physical values zero and \texttt{rho}$=1$; no relation one-hot \\
    \midrule
    \texttt{heterogeneous} & Physical busbar $\leftrightarrow$ busbar & Complete measured physical-line block plus \texttt{relation\_physical\_line} \\
     & Generator $\leftrightarrow$ busbar & Neutral physical block plus direction-specific generator relation \\
     & Load $\leftrightarrow$ busbar & Neutral physical block plus direction-specific load relation \\
     & Optional self or same-substation & Neutral physical block plus the corresponding relation \\
    \midrule
    \texttt{heterogeneous\_line} & Busbar $\leftrightarrow$ line node & One origin/extremity-and-direction relation; no line-state attributes \\
     & Generator $\leftrightarrow$ busbar & One direction-specific generator relation \\
     & Load $\leftrightarrow$ busbar & One direction-specific load relation \\
     & Optional self or same-substation & One corresponding relation \\
    \midrule
    Any augmented schema & Busbar $\rightarrow$ summary/virtual, with optional reverse & Zero edge vector except neutral \texttt{rho}$=1$ when present; no dedicated relation one-hot in the ordinary GINE/GAT encoder \\
    \bottomrule
  \end{tabular}
\end{table}

The complete relation names and activation rules are given in
Tables~\ref{tab:heterogeneous-edge-features}
and~\ref{tab:heterogeneous-line-edge-features}. Those tables distinguish, for
example, generator-to-busbar from busbar-to-generator and origin from
extremity line attachments. These distinctions matter directly to both
edge-aware models: GINE changes message content and GAT changes attention.

\chapter{Graph Construction Details}
\label{app:graph-construction}

{\color{blue}This appendix collects the construction and sizing details of the three graph
schemas of Section~\ref{sec:graph-representation}: how large each candidate
graph is, how inactive candidates are neutralized, and how a local actor graph
is cut out of the full grid. None of it changes the representations themselves.

\section{Candidate graph sizes}
\label{app:graph-sizes}

Let $S$ be the number of included substations, $B$ the busbars per substation,
$L$ the included physical lines, and $N_g$, $N_d$ the generators and loads.
Write $d_g,d_d,d_{\ell}\in\{1,2\}$ for the number of retained directions of the
generator, load, and line-node attachments: $2$ for \texttt{bidirectional} and
$1$ for either one-way mode. Let $P_{\ell}$ be the number of represented line
endpoints: an internal line contributes two and a boundary line in a local
explicit-line graph contributes only its controlled endpoint. Thus
$L\leq P_{\ell}\leq 2L$. Table~\ref{tab:appendix-graph-sizes} gives the size of
the fixed candidate graph before any mask is applied.

\begin{table}[H]
  \centering
  \small
  \caption{Size of the candidate graph of each schema. Enabling self edges adds
  $|\mathcal{V}|$ directed edges and enabling same-substation edges adds
  $SB(B-1)$, in every schema.}
  \label{tab:appendix-graph-sizes}
  \setlength{\tabcolsep}{5pt}
  \begin{tabular}{L{0.24\textwidth}L{0.26\textwidth}L{0.38\textwidth}}
    \toprule
    Schema & Candidate nodes & Candidate directed edges \\
    \midrule
    \texttt{bus} & $SB$ & $2B^2L$ \\
    \texttt{heterogeneous} & $SB+N_g+N_d$ & $2B^2L + B(d_gN_g+d_dN_d)$ \\
    \texttt{heterogeneous\_line} & $SB+N_g+N_d+L$ & $Bd_{\ell}P_{\ell} + B(d_gN_g+d_dN_d)$ \\
    \bottomrule
  \end{tabular}
\end{table}

Three consequences are worth naming. A busbar graph with both optional
relations enabled therefore has $2B^2L+SB+SB(B-1)$ candidate directed edges.
The disaggregated schema adds $N_g+N_d$ nodes and between $B(N_g+N_d)$ and
$2B(N_g+N_d)$ asset candidates to the busbar graph, and buys individual asset
states with them. For a complete state graph, $P_{\ell}=2L$, so the
explicit-line schema replaces the $2B^2L$ busbar-to-busbar term by
$2Bd_{\ell}L$: identical at $B=2$ and cheaper for $B>2$. A local boundary line
has candidates only at its controlled endpoint, which reduces $P_{\ell}$ and
prevents construction of a far-end attachment. The schema adds $L$ line nodes
and one message-passing hop for internal endpoint exchange.

The hierarchical augmentations of Section~\ref{sec:substation-representation}
add on top of any of these: one substation-summary node per substation, with
$SB$ directed edges for \texttt{toward\_summary} or $2SB$ for
\texttt{bidirectional}; and one virtual node, with $N_{\mathrm{bus}}$ or
$2N_{\mathrm{bus}}$ edges, or $S$ or $2S$ when summary nodes are also enabled.

\section{Indexing and masking conventions}
\label{app:graph-indexing}

Grid2Op numbers busbars from one in its topology vector; those identifiers are
converted to the zero-based index $b$ used in
Equation~\ref{eq:busbar-node-id}.

An inactive candidate edge keeps its row in the edge-feature tensor, so the
tensor shape is fixed for an episode, but the row is zero-filled and its
\texttt{edge\_mask} entry is $0$. Masked edges are removed before the encoder
is called, so those zero rows never generate a message. This is what allows a
topology change to alter connectivity without rebuilding the graph.
An inactive contextual node likewise retains its candidate row, but its
complete feature vector is zero and its \texttt{node\_mask} entry is $0$.
Every incident physical or structural edge is deactivated. The row therefore
participates in neither message passing nor graph readout; a mean readout
excludes it from both numerator and denominator.

\section{Local actor graphs}
\label{app:local-graphs}

A local actor graph always contains the controlled substations and every line
touching them. Additional membership is schema-specific. The \texttt{bus}
graph contains only the far-end busbar currently attached by each live boundary
line, and that busbar exposes aggregated neighboring power and angle. The
direct \texttt{heterogeneous} graph contains the same far-end busbar, but never
its generator or load nodes. The \texttt{heterogeneous\_line} graph represents
the shared line itself and therefore constructs no neighboring busbar or
private asset and no far-end attachment edge. The centralized state graph is
intentionally unrestricted for centralized training. Ordinary pooling
aggregates all visible nodes, whereas \texttt{controlled\_*} pooling restricts
readout to action-domain nodes; neither choice changes the construction
boundary.

\section{Boundary verification}

The regression suite checks that contextual visibility follows a live busbar
attachment, moves when the attachment is switched, and disappears after a
line outage; controlled nodes remain present; structural relations cannot make
a hidden candidate visible; inactive rows are zero; neighboring private assets
are absent from direct heterogeneous graphs; and all neighboring equipment is
absent from explicit-line local graphs. Candidate-action tests additionally
verify that a boundary line remains addressable through its line node without
adding far-end equipment. Controlled-readout tests verify both that the pooled
set is fixed and that gradients can still reach visible context through message
passing.}

\chapter{Experiment Configurations}
\label{app:experiment-configurations}

This appendix provides the complete run configurations for the GINE encoder and heavy GINE ablations.

\begin{landscape}
\begin{table}[p]
  \centering
  \scriptsize
  \caption{GINE rerun configuration summary. The reference run is
  \texttt{Heavy GINE Baseline}.}
  \label{tab:gine-run-configurations}
  \setlength{\tabcolsep}{2.4pt}
  \renewcommand{\arraystretch}{1.12}
  \begin{tabular}{L{0.18\linewidth}L{0.075\linewidth}L{0.065\linewidth}L{0.095\linewidth}L{0.050\linewidth}L{0.060\linewidth}L{0.050\linewidth}L{0.065\linewidth}L{0.070\linewidth}L{0.19\linewidth}}
    \toprule
    Run & Actor/critic encoder & GNN hidden/layers & Actor/critic heads &
    Concat flat & Edge pre-enc. & Entropy & Init action-0 & Opt. critic &
    Difference from \texttt{Heavy GINE Baseline} \\
    \midrule
    \texttt{Heavy GINE Baseline} & \texttt{gnn} / \texttt{gnn} & 128 / 2 &
    \(3\times256\) / \(3\times256\) & true & true & 0.02 & 0.7 & false &
    Baseline: heavy concat-flat GINE actor, GNN critic, and legacy critic updates \\
    \texttt{Light GINE (Concat, MLP Critic)} & \texttt{gnn} / \texttt{mlp} & 64 / 1 &
    \(2\times128\) / \(2\times128\) & true & false & 0.02 & 0.7 & true &
    Light GINE actor, MLP critic, smaller heads, no edge pre-encoder, optimized critic updates \\
    \texttt{Heavy GINE (No Bias)} & \texttt{gnn} / \texttt{gnn} & 128 / 2 &
    \(3\times256\) / \(3\times256\) & true & true & 0.02 & 0.0 & false &
    Bias ablation: same as baseline, but the initial do-nothing prior is 0.0 \\
    \texttt{No Bias, Opt Critic} & \texttt{gnn} / \texttt{gnn} & 128 / 2 &
    \(3\times256\) / \(3\times256\) & true & true & 0.02 & 0.0 & true &
    Bias 0.0 plus optimized critic updates; otherwise heavy concat-flat GINE with GNN critic \\
    \texttt{Optimized Heavy GINE} & \texttt{gnn} / \texttt{gnn} & 128 / 2 &
    \(3\times256\) / \(3\times256\) & true & true & 0.01 & 0.0 & true &
    Optimized heavy GINE: bias 0.0, lower entropy 0.01, optimized critic updates \\
    \texttt{Optimized Light GINE} & \texttt{gnn} / \texttt{mlp} & 64 / 1 &
    \(2\times128\) / \(2\times128\) & false & false & 0.01 & 0.0 & true &
    Light optimized GINE: MLP critic, no concat-flat, lower entropy 0.01, bias 0.0, optimized critic updates \\
    \bottomrule
  \end{tabular}
\end{table}
\end{landscape}

\begin{landscape}
\begin{table}[p]
  \centering
  \tiny
  \caption{Heavy-GINE parameter comparison from
  \texttt{configs/gine\_s0\_s1\_s2}.}
  \label{tab:heavy-gine-configurations}
  \setlength{\tabcolsep}{2pt}
  \renewcommand{\arraystretch}{1.12}
  \begin{tabular}{L{0.10\linewidth}L{0.18\linewidth}L{0.045\linewidth}L{0.060\linewidth}L{0.050\linewidth}L{0.075\linewidth}L{0.045\linewidth}L{0.045\linewidth}L{0.045\linewidth}L{0.045\linewidth}L{0.060\linewidth}L{0.045\linewidth}L{0.055\linewidth}}
    \toprule
    Run & Main change from baseline & GNN & GNN size & Critic &
    Actor/critic heads & Concat & Shared & Edge pre. & Node ID & Entropy &
    Init action-0 & Opt. critic \\
    \midrule
    \texttt{No Flat Concat} & Removes flat-observation concatenation &
    \texttt{gine} & \(2\times128\) & \texttt{gnn} & \(3\times256\) / \(3\times256\) &
    no & yes & yes & yes & 0.02--0.02 & 0.7 & no \\
    \texttt{Heavy GINE Baseline} & Reference: heavy concat-flat GINE with GNN critic &
    \texttt{gine} & \(2\times128\) & \texttt{gnn} & \(3\times256\) / \(3\times256\) &
    yes & yes & yes & yes & 0.02--0.02 & 0.7 & no \\
    \texttt{Opt. Critic Updates} & Enables optimized critic updates &
    \texttt{gine} & \(2\times128\) & \texttt{gnn} & \(3\times256\) / \(3\times256\) &
    yes & yes & yes & yes & 0.02--0.02 & 0.7 & yes \\
    \texttt{MLP Critic} & Replaces the GNN critic with an MLP critic &
    \texttt{gine} & \(2\times128\) & \texttt{mlp} & \(3\times256\) / \(3\times256\) &
    yes & yes & yes & yes & 0.02--0.02 & 0.7 & no \\
    \texttt{Unshared Actor} & Uses non-shared actor GNN weights &
    \texttt{gine} & \(2\times128\) & \texttt{gnn} & \(3\times256\) / \(3\times256\) &
    yes & no & yes & yes & 0.02--0.02 & 0.7 & no \\
    \texttt{Light GINE Encoder} & Uses a lighter GINE encoder &
    \texttt{gine} & \(1\times64\) & \texttt{gnn} & \(3\times256\) / \(3\times256\) &
    yes & yes & yes & yes & 0.02--0.02 & 0.7 & no \\
    \texttt{Zero Entropy Decay} & Decays entropy from 0.02 to 0.0 &
    \texttt{gine} & \(2\times128\) & \texttt{gnn} & \(3\times256\) / \(3\times256\) &
    yes & yes & yes & yes & 0.02--0.00 & 0.7 & no \\
    \texttt{No Node-ID Embs} & Removes learned node-ID embeddings &
    \texttt{gine} & \(2\times128\) & \texttt{gnn} & \(3\times256\) / \(3\times256\) &
    yes & yes & yes & no & 0.02--0.02 & 0.7 & no \\
    \texttt{GAT Message Passing} & Replaces GINE with GAT message passing &
    \texttt{gat} & \(2\times128\) & \texttt{gnn} & \(3\times256\) / \(3\times256\) &
    yes & yes & yes & yes & 0.02--0.02 & 0.7 & no \\
    \texttt{Weighted GCN} & Replaces GINE with weighted GCN message passing &
    \texttt{gcn} & \(2\times128\) & \texttt{gnn} & \(3\times256\) / \(3\times256\) &
    yes & yes & yes & yes & 0.02--0.02 & 0.7 & no \\
    \texttt{Light GINE (No Concat, MLP Critic)} & Light no-concat GINE with MLP critic and optimized critic updates &
    \texttt{gine} & \(1\times64\) & \texttt{mlp} & \(2\times128\) / \(2\times128\) &
    no & yes & no & yes & 0.02--0.02 & 0.7 & yes \\
    \texttt{Light GINE (Concat, MLP Critic)} & Light concat-flat GINE with MLP critic and optimized critic updates &
    \texttt{gine} & \(1\times64\) & \texttt{mlp} & \(2\times128\) / \(2\times128\) &
    yes & yes & no & yes & 0.02--0.02 & 0.7 & yes \\
    \texttt{No Initial Bias} & Removes the initial do-nothing bias &
    \texttt{gine} & \(2\times128\) & \texttt{gnn} & \(3\times256\) / \(3\times256\) &
    yes & yes & yes & yes & 0.02--0.02 & 0.0 & no \\
    \bottomrule
  \end{tabular}
\end{table}
\end{landscape}

\chapter{Full-Test Checkpoint Registry}
\label{app:full-test-checkpoint-registry}

Table~\ref{tab:full-test-checkpoint-steps} reports the checkpoint step used for
each full-test result table. The values are the
\path{checkpoint_global_step} fields stored in the corresponding JSON files
under \path{Topology_Task/outputs/full_test_eval}. The do-nothing policy has
no learned checkpoint. For the behavioral cloning students, the stored step is
the student checkpoint's optimizer-step count rather than a MAPPO environment
step.

\begin{landscape}
\begin{table}[htbp]
  \centering
  \small
  \caption{Checkpoint steps used for the full-test evaluations.}
  \label{tab:full-test-checkpoint-steps}
  \setlength{\tabcolsep}{5pt}
  \resizebox{\linewidth}{!}{%
  \begin{tabular}{L{0.54\linewidth}rrr}
    \toprule
    Run type & Seed 0 checkpoint step & Seed 1 checkpoint step &
    Seed 2 checkpoint step \\
    \midrule
    Do-nothing policy & -- & -- & -- \\
    Baseline flat policy & 13{,}600{,}000 & 13{,}040{,}000 & 13{,}760{,}000 \\
    Global rho heuristic, \(\rho_{safe}=0.90\) & 13{,}920{,}000 & 14{,}320{,}000 & 14{,}800{,}000 \\
    Local rho heuristic, \(\rho_{safe}=0.90\) & 14{,}160{,}000 & 14{,}400{,}000 & 14{,}160{,}000 \\
    Intervention gate, final-action MAP & 12{,}000{,}000 & 12{,}720{,}000 & 13{,}760{,}000 \\
    Intervention gate, hierarchical greedy & 7{,}120{,}000 & 14{,}720{,}000 & 13{,}760{,}000 \\
    Flat Sparse16, \(\lambda_{fixed}=0.000\) & 9{,}600{,}000 & 12{,}160{,}000 & 7{,}920{,}000 \\
    Flat Sparse16, \(\lambda_{fixed}=0.001\) & 10{,}640{,}000 & 11{,}520{,}000 & 11{,}600{,}000 \\
    Flat Sparse16, \(\lambda_{fixed}=0.003\) & 12{,}000{,}000 & 12{,}960{,}000 & 12{,}480{,}000 \\
    Flat Sparse16, \(\lambda_{fixed}=0.010\) & 8{,}960{,}000 & 5{,}760{,}000 & 12{,}560{,}000 \\
    Gated Sparse16, \(\lambda_{fixed}=0.000\) & 8{,}480{,}000 & 4{,}320{,}000 & 6{,}000{,}000 \\
    Gated Sparse16, \(\lambda_{fixed}=0.001\) & 5{,}760{,}000 & 7{,}600{,}000 & 3{,}280{,}000 \\
    Gated Sparse16, \(\lambda_{fixed}=0.003\) & 8{,}960{,}000 & 7{,}120{,}000 & 7{,}600{,}000 \\
    Gated Sparse16, \(\lambda_{fixed}=0.010\) & 12{,}000{,}000 & 7{,}840{,}000 & 7{,}920{,}000 \\
    Flat local-safe AIB, \(d=0.20\) & 13{,}120{,}000 & 9{,}360{,}000 & 12{,}160{,}000 \\
    Flat local-safe AIB, \(d=0.10\) & 12{,}880{,}000 & 13{,}200{,}000 & 12{,}720{,}000 \\
    Flat local-safe AIB, \(d=0.35\) & 12{,}800{,}000 & 5{,}360{,}000 & 11{,}520{,}000 \\
    Gated local-safe AIB, \(d=0.20\), hierarchical greedy & 14{,}960{,}000 & 5{,}280{,}000 & 16{,}800{,}000 \\
    Flat non-idle AIB, \(d=0.20\) & 12{,}480{,}000 & 14{,}880{,}000 & 11{,}920{,}000 \\
    Teacher: MAPPO with local rho heuristic & 14{,}160{,}000 & 14{,}400{,}000 & 14{,}160{,}000 \\
    Student BC, balanced non-idle 0.50, weight 5, aux 0.50 & 12{,}375 & 14{,}994 & 13{,}617 \\
    Student BC, balanced non-idle 0.20, weight 3, aux 0.25 & 12{,}375 & 14{,}994 & 13{,}617 \\
    \bottomrule
  \end{tabular}
  }
\end{table}
\end{landscape}

\nocite{*}
\printbibliography

\end{document}
